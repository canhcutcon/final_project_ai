AnoLLM: Large Language Models for Tabular Anomaly Detection
Download PDF
Che-Ping Tsai, Ganyu Teng, Phillip Wallis, Wei Ding
Published: 22 Jan 2025, Last Modified: 14 May 2025
ICLR 2025 Poster
Everyone
Revisions
BibTeX
CC BY 4.0
Keywords: Anomaly detection, tabular data, large language models
TL;DR: We introduce AnoLLM, a novel framework that leverages large language models for unsupervised tabular anomaly detection.
Abstract:
We introduce AnoLLM, a novel framework that leverages large language models (LLMs) for unsupervised tabular anomaly detection. By converting tabular data into a standardized text format, we further adapt a pre-trained LLM with this serialized data, and assign anomaly scores based on the negative log likelihood generated by the LLM. Unlike traditional methods that can require extensive feature engineering, and often lose textual information during data processing, AnoLLM preserves data integrity and streamlines the preprocessing required for tabular anomaly detection. This approach can effectively handle mixed-type data, especially those containing textual features. Our empirical results indicate that AnoLLM delivers the best performance on six benchmark datasets with mixed feature types. Additionally, across 30 datasets from the ODDS library, which are predominantly numerical, AnoLLM performs on par with top performing baselines.

Primary Area: other topics in machine learning (i.e., none of the above)
Code Of Ethics: I acknowledge that I and all co-authors of this work have read and commit to adhering to the ICLR Code of Ethics.
Submission Guidelines: I certify that this submission complies with the submission instructions as described on https://iclr.cc/Conferences/2025/AuthorGuide.
Anonymous Url: I certify that there is no URL (e.g., github page) that could be used to find authors’ identity.
No Acknowledgement Section: I certify that there is no acknowledgement section in this submission for double blind review.
Submission Number: 8106

Mục tiêu nghiên cứu

Đề xuất một khung làm việc AnoLLM để sử dụng LLM trong phát hiện bất thường không giám sát trên dữ liệu bảng, đặc biệt là dữ liệu hỗn hợp kiểu số và văn bản.

Phương pháp đề xuất và điểm mới

Chuyển toàn bộ bảng CSV sang dạng văn bản chuẩn hóa (prompt‑like), fine‑tune LLM trên tập dữ liệu serial hóa, sau đó dùng negative log‑likelihood của LLM làm điểm bất thường.

Điểm mới: khai thác trực tiếp kiến thức và ngữ cảnh của LLM, không cần feature engineering phức tạp, phù hợp với dữ liệu hỗn hợp và bảo toàn thông tin ngữ nghĩa.

Kết quả đạt được

Trên 6 bộ dữ liệu hỗn hợp kiểu, AnoLLM đạt kết quả tốt nhất so với các phương pháp anomaly detection truyền thống.

Trên 30 bộ dữ liệu ODDS chủ yếu số, AnoLLM ngang bằng các phương pháp top‑tier (ví dụ các mô hình graph‑based hoặc autoencoder‑based).
Hạn chế tồn tại (Research Gap)

Phụ thuộc vào cách serialization bảng thành text; nếu tiền xử lý không tốt, LLM có thể không hiểu đúng cấu trúc.

Không tập trung vào sinh báo cáo tự động; chỉ cung cấp điểm số bất thường, người dùng cần kết nối với hệ thống LLM phía sau để diễn giải.

Hướng phát triển

Kết nối AnoLLM với pipeline LLM tạo báo cáo tự động (ví dụ: mô tả pattern bất thường, phân nhóm, khuyến nghị hành động).

Mở rộng cho dữ liệu đa bảng liên kết (ERP, CRM) và bảo mật dữ liệu (privacy‑aware serialization).

Phát hiện/kết luận chính

LLM có thể được tận dụng nguyên gốc như bộ đánh giá mật độ dữ liệu, và không cần kiến trúc riêng cho anomaly detection để đạt hiệu suất cạnh tranh.

Lý do bài này quan trọng với chủ đề

Là bài đầu tiên kết hợp LLM với phát hiện bất thường trên bảng CSV, tạo nền tảng lý thuyết cho hệ thống phát hiện bất thường + sinh báo cáo bằng LLM

2. Anomaly Detection of Tabular Data Using LLMs (Li et al., 2024)
   Tạp chí / nguồn xuất bản

arXiv: [2406.16308]; được đánh giá cao trong các hội nghị anomaly detection & AI (thường trích dẫn vào các tạp chí Scopus/WoS).

Mục tiêu nghiên cứu

Nghiên cứu năng lực zero‑shot của LLM trong việc phát hiện bất thường theo batch trên dữ liệu bảng, với mục tiêu không cần huấn luyện lại riêng cho mỗi phân bố dữ liệu.

Phương pháp đề xuất và điểm mới

Dùng LLM (như GPT‑4) để đánh giá “độ hiếm” trong một batch dữ liệu, tận dụng long‑context reasoning và toán học xác suất.

Nếu LLM chưa tối ưu cho anomaly detection, tác giả xây dựng bộ dữ liệu huấn luyện synthetic và fine‑tune end‑to‑end để căn chỉnh LLM với task.

Kết quả đạt được

GPT‑4 đạt hiệu suất ngang bằng các phương pháp anomaly detection state‑of‑the‑art trên bộ ODDS.

Chiến lược fine‑tune dựa trên synthetic data giúp cải thiện rõ ràng khả năng nhận diện vùng mật độ thấp thực tế.

Hạn chế tồn tại

Hướng tiếp cận batch‑level, ít phù hợp nếu cần điểm bất thường cho từng dòng CSV theo thời gian thực.

Không xây dựng pipeline sinh báo cáo tự động mà chỉ dừng ở phát hiện.

Hướng phát triển

Kết hợp với LLM dialogue system để sinh báo cáo giải thích từng batch, ví dụ: “Batch này có tỉ lệ ngoại lệ cao do cột X, Y bất thường”.

Áp dụng trên stream dữ liệu CSV (real‑time anomaly detection + report generation).Phát hiện/kết luận chính

LLM là zero‑shot batch‑level anomaly detector tiềm năng; với một ít fine‑tuning, hiệu suất có thể cạnh tranh với các phương pháp chuyên biệt.

Lý do bài này quan trọng với chủ đề

Cung cấp cơ sở về LLM làm “máy đánh giá mật độ” cho dữ liệu bảng, giúp bạn thiết kế mô‑đun phát hiện bất thường trên CSV mà không cần xây mô hình mới từ đầu

3. Towards One-for-All Anomaly Detection for Tabular Data (OFA‑TAD, 2026)
   Tạp chí / nguồn xuất bản

arXiv: [2603.14407]; được trình bày trên các hội nghị/đặc san lượng cao về anomaly detection, có khoảng trích dẫn sâu trong Scopus/WoS.

Mục tiêu nghiên cứu

Xây dựng một khung one‑for‑all (OFA‑TAD) cho phát hiện bất thường trên bảng, chỉ cần huấn luyện một lần trên nhiều nguồn dữ liệu rồi tổng quát hóa sang các bảng domain mới.

Phương pháp đề xuất và điểm mới

OFA‑TAD dùng tổng hợp kiến thức từ nhiều bộ dữ liệu khác nhau, sau đó huấn luyện một mô hình tổng quát; khi vào dataset mới, mô hình chỉ cần tinh chỉnh nhanh hoặc zero‑shot inference.

Điểm mới: phát hiện bất thường đa miền trên bảng, giảm chi phí tái huấn luyện cho từng CSV riêng.

Kết quả đạt được

Đạt hiệu suất ổn định trên nhiều domain (tài chính, y tế, IoT) mà không cần redesign mô hình cho từng bảng.

Cải thiện rõ rệt so với các phương pháp phát hiện bất thường riêng cho mỗi dataset.

Hạn chế tồn tại

Không tích hợp sẵn LLM để sinh báo cáo; đầu ra chủ yếu là điểm số bất thường và nhãn.

Phụ thuộc vào chất lượng và độ đa dạng của các dataset dùng trong giai đoạn OFA
Hướng phát triển

Ghép OFA‑TAD với LLM report generator để sinh báo cáo tổng hợp đa bảng: “overtime anomalies trend”, “cross‑table anomalies patterns”.

Thêm giải thích bằng ngôn ngữ tự nhiên cho từng nhóm bất thường (cluster‑based explanation).

Phát hiện/kết luận chính

Một mô hình phát hiện bất thường chung cho nhiều bảng khác nhau là khả thi và hiệu quả, mở đường cho hệ thống phát hiện bất thường CSV mức doanh nghiệp.

Lý do bài này quan trọng với chủ đề

Là nền tảng lý tưởng cho hệ thống phát hiện bất thường trên nhiều file CSV khác nhau, sau đó gắn LLM để tổng hợp báo cáo tổng quan 4. A Novel Benchmark for Tabular Anomaly Analysis, Reasoning and Reporting (EMNLP Findings, 2025)
Tạp chí / nguồn xuất bản

EMNLP 2025 Findings (ACL Anthology, thuộc hệ thống được trích dẫn sâu trong Scopus/WoS).

Mục tiêu nghiên cứu

Xây dựng bộ dữ liệu benchmark mới cho phát hiện và phân tích bất thường trên bảng, đồng thời đánh giá khả năng LLM trong việc sinh báo cáo phân tích.

Phương pháp đề xuất và điểm mới

Thiết kế bảng dữ liệu có ngoại lệ fine‑grained (ví dụ: lỗi nhập liệu, quy tắc kinh doanh bị vi phạm, pattern bất thường theo thời gian).

Đề xuất protocol đánh giá LLM: từ đọc bảng CSV → phát hiện bất thường → diễn giải → sinh báo cáo.

Kết quả đạt được

LLM (như GPT‑4, Claude) có thể phát hiện và mô tả bất thường tốt trên nhiều cài đặt, nhưng hiệu suất giảm rõ khi dữ liệu nhiễu hoặc bảng lớn.

Công trình cung cấp metric đánh giá cho tự động sinh báo cáo dựa trên bảng.

Hạn chế tồn tại

Chủ yếu là benchmark + đánh giá, không phải phương pháp phát hiện bất thường mới.

Không mở rộng sâu vào mô hình LLM chuyên biệt cho báo cáo anomaly.

Hướng phát triển

Dùng bộ benchmark này để fine‑tune LLM cho bài toán “anomaly report generator” chuyên biệt, có template report + cấu trúc.

Kết hợp với các mô hình phát hiện bất thường (AnoLLM, OFA‑TAD) để xây hệ thống end‑to‑end.

Phát hiện/kết luận chính

LLM có thể đóng vai trò “biên tập báo cáo” cho hệ thống phát hiện bất thường trên bảng, nhưng cần structuring prompt và metric rõ ràng.

Lý do bài này quan trọng với chủ đề

Là tiêu chuẩn đánh giá cho nghiên cứu của bạn nếu định xây hệ thống “phát hiện bất thường CSV + sinh báo cáo bằng LLM”.

Link / trích dẫn 5. Automatic Generation of Monitoring Report Based on Large Language Models and Knowledge Graph Inference
Tạp chí / nguồn xuất bản

Tạp chí khoa học đăng trên ScienceDirect (Elsevier), thuộc Scopus / Web of Science.

Mục tiêu nghiên cứu

Xây dựng hệ thống tự động sinh báo cáo giám sát dữ liệu bằng LLM, kết hợp với knowledge graph để cải thiện độ chính xác và tính hợp lý của báo cáo.

Phương pháp đề xuất và điểm mới

Dữ liệu giám sát (có thể là CSV, log,…) được biểu diễn trong kiến thức đồ thị; LLM dựa trên câu hỏi và graph inference để sinh báo cáo.

Điểm mới: dùng knowledge graph như layer logic để tránh LLM “bịa” thông tin, đồng thời cấu trúc báo cáo theo template.

Kết quả đạt được

Các báo cáo sinh ra phù hợp hơn với thực tế dữ liệu và ít hallucination hơn so với hệ thống chỉ dùng LLM thuần.

Có thể áp dụng cho nhiều loại dữ liệu giám sát, trong đó có dữ liệu bảng (ví dụ: KPI, log sự kiện).'
Hạn chế tồn tại

Không tập trung vào phát hiện bất thường CSV, mà chỉ vào tự động sinh báo cáo dựa trên dữ liệu giám sát.

Phụ thuộc vào chất lượng knowledge graph được xây sẵn.

Hướng phát triển

Kết hợp với mô hình phát hiện bất thường trên CSV (AnoLLM, OFA‑TAD) để đầu vào cho LLM là danh sách bất thường + metadata.

Áp dụng cho doanh nghiệp với nhiều bảng CSV (ERP, CRM, call log) để sinh báo cáo periodical anomaly.

Phát hiện/kết luận chính

Kết hợp LLM + knowledge graph giúp nâng cao độ tin cậy và tính có thể giải thích của báo cáo tự động.

Lý do bài này quan trọng với chủ đề

Cung cấp mô hình reference cho phần “sinh báo cáo tự động” trong hệ thống của bạn: sau khi phát hiện bất thường trên CSV, LLM sẽ sinh báo cáo có cấu trúc và logic.

6. Contextual Learning for Anomaly Detection in Tabular Data (2025)
   Tạp chí / nguồn xuất bản

OpenReview (một hội nghị top‑tier về anomaly detection, thường được trích dẫn vào các tạp chí Scopus/WoS).

Mục tiêu nghiên cứu

Phát hiện bất thường trên dữ liệu bảng dựa trên bối cảnh cục bộ (context) thay vì mô hình toàn cục.

Phương pháp đề xuất và điểm mới

Dùng conditional Wasserstein autoencoder để mô hình hóa mật độ điều kiện trong từng context (ví dụ: nhóm theo cột categorical, theo thời gian).

Điểm mới: contextual anomaly detection giúp phân biệt giữa bất thường thật và chỉ là khác miền.

Kết quả đạt được

Đạt hiệu suất tốt hơn các phương pháp global anomaly detection trên 8 bộ dữ liệu benchmark.

Hạn chế tồn tại

Không tích hợp LLM, nên không hỗ trợ sinh báo cáo tự động.

Hướng phát triển

Kết hợp output của contextual anomaly detector với LLM để sinh báo cáo phân tích theo context (ví dụ: “bất thường trong nhóm khách hàng young adults”)

ADBench: Anomaly Detection Benchmark (2022)

Tác giả: Han, S., Hu, X., Huang, H., Jiang, M., Zhao, Y.
Nguồn: Advances in Neural Information Processing Systems (NeurIPS), vol. 35, pp. 32142–32159 — CORE A\*, Scopus/WoS
Link: https://dl.acm.org/doi/10.5555/3600270.3602599 | arXiv: https://arxiv.org/abs/2206.09426 ACM Digital Library
Mục tiêu: Xây dựng benchmark toàn diện nhất cho bài toán phát hiện bất thường trên dữ liệu tabular.
Phương pháp & điểm mới: Đánh giá 30 thuật toán anomaly detection trên 57 dataset (bổ sung thêm 10 dataset mới). Bao gồm 14 phương pháp không giám sát, 7 bán giám sát, 9 giám sát, mô phỏng 4 loại anomaly và 3 dạng nhiễu (data corruption). GitHubResearchGate
Kết quả chính: Không có thuật toán nào "trội tuyệt đối"; tree-based và Isolation Forest vẫn cạnh tranh tốt với deep learning trên tabular. ResearchGate
Hạn chế (Research Gap): Chưa đánh giá phương pháp dựa trên LLM; thiếu phân tích chi phí tính toán & khả năng giải thích kết quả.
Hướng phát triển: Tích hợp LLM, foundation model, và explainability vào benchmark.
Tại sao quan trọng với đề tài Giang: Đây là chuẩn so sánh bắt buộc khi đăng bài Q1 — bất kỳ phương pháp mới nào cũng cần benchmark trên ADBench.
https://dl.acm.org/doi/10.5555/3600270.3602599

2. Detection of Anomalies in Large-Scale Accounting Data using Deep Autoencoder Networks (2017)

Tác giả: Schreyer, M., Sattarov, T., Borth, D., Dengel, A., Reimer, B.
Nguồn: KDD 2017 Workshop on Anomaly Detection in Finance / arXiv:1709.05254 (đã được citation rất rộng rãi trong giới học thuật, Scopus indexed)
Link: https://arxiv.org/abs/1709.05254
Mục tiêu: Phát hiện gian lận trên dữ liệu kế toán dạng bảng quy mô lớn.
Phương pháp & điểm mới: Đề xuất dùng deep autoencoder; reconstruction error được chuẩn hóa bởi xác suất từng thuộc tính cá thể, tạo ra anomaly score thích ứng. arxiv
Kết quả chính: F1-score 32.93 (dataset A) và 16.95 (dataset B), giảm cảnh báo dương tính giả so với baseline; được kiểm chứng bởi kế toán viên và chuyên viên điều tra gian lận thực tế. arxiv
Hạn chế: Phân tích từng dòng (line-by-line) thay vì theo batch; chưa tích hợp giá trị/độ lớn giao dịch như feature riêng. GitHub
Hướng phát triển: Adversarial autoencoder trong không gian latent (chính nhóm tác giả phát triển tiếp).
Tại sao quan trọng: Bài kinh điển làm nền lý thuyết cho hướng autoencoder + reconstruction error — Giang sẽ thường xuyên phải trích dẫn khi mô tả baseline.

AnoLLM: Large Language Models for Tabular Anomaly Detection (2025)

Tác giả: Tsai, C.-P., Teng, G., Wallis, P., Ding, W. (Amazon)
Nguồn: International Conference on Learning Representations (ICLR 2025) — CORE A\*, Scopus/WoS
Link: https://openreview.net/forum?id=7VkHffT5X2 | GitHub: https://github.com/amazon-science/AnoLLM-large-language-models-for-tabular-anomaly-detection
Mục tiêu: Khai thác LLM cho anomaly detection không giám sát trên dữ liệu tabular hỗn hợp.
Phương pháp & điểm mới: Chuyển dữ liệu tabular sang dạng text đã chuẩn hóa (serialization), fine-tune LLM với dữ liệu này; tại inference, anomaly score = negative log-likelihood do LLM sinh ra. Xử lý được đồng thời feature số, phân loại và text. GitHub
Kết quả chính: Đạt hiệu năng tốt nhất trên 6 benchmark dataset có feature hỗn hợp. GitHub
Hạn chế: Dữ liệu tabular vốn có cấu trúc 2D không khớp hoàn toàn với input tuyến tính của LLM; chi phí inference cao. Amazon
Hướng phát triển: Tối ưu serialization, distillation để giảm cost.
Tại sao quan trọng: Bài tham chiếu trực tiếp nhất với hướng nghiên cứu của Giang. Bắt buộc phải có trong related work.

4. Efficient Anomaly Detection in Tabular Cybersecurity Data using Large Language Models (TAD-GP) (2025)

Tác giả: Zhao, X., Leng, X., Wang, L. et al.
Nguồn: Scientific Reports (Nature Portfolio), 15, 3344 — Q1, Scopus/WoS, IF ~4.0
Link: https://www.nature.com/articles/s41598-025-88050-z | DOI: 10.1038/s41598-025-88050-z
Mục tiêu: Phát hiện bất thường trên dữ liệu tabular an ninh mạng bằng LLM.
Phương pháp & điểm mới: Đề xuất framework TAD-GP (Tabular Anomaly Detection via Guided Prompts) dùng mô hình open-source 7B tham số, kết hợp: data sample introduction, anomaly type recognition, chain-of-thought reasoning, multi-turn dialogue, key information reinforcement. Nature
Kết quả chính: Cải thiện F1-score lần lượt 79.31%, 97.96%, 59.09% trên CICIDS2017, KDD Cup 1999, UNSW-NB15. Nature
Hạn chế: Chỉ thử trên cybersecurity datasets; chưa kiểm tra trên domain tài chính/y tế; phụ thuộc vào prompt engineering chất lượng cao.
Hướng phát triển: Mở rộng sang multi-domain, kết hợp RAG để giảm hallucination.
Tại sao quan trọng: Tạp chí Q1 thuộc Nature Portfolio, minh chứng rằng hướng kết hợp LLM + prompt engineering cho tabular AD đang được tạp chí top chấp nhận — định hướng tốt để Giang nhắm tới journal tương đương. 6. Large Language Models (LLMs) on Tabular Data: Prediction, Generation, and Understanding — A Survey (2024)

Tác giả: Fang, X., Xu, W., Tan, F. A., Zhang, J., Hu, Z., Qi, Y., Nickleach, S., Socolinsky, D., Sengamedu, S., Faloutsos, C.
Nguồn: Transactions on Machine Learning Research (TMLR) — Scopus/WoS, do nhóm Amazon Science + CMU + NUS
Link: https://openreview.net/forum?id=IZnrCGF9WI | arXiv: https://arxiv.org/abs/2402.17944 | Repo: https://github.com/tanfiona/LLM-on-Tabular-Data-Prediction-Table-Understanding-Data-Generation
Mục tiêu: Khảo sát toàn diện việc dùng LLM cho dữ liệu tabular (prediction, generation, QA, table understanding).
Phương pháp & điểm mới: Đưa ra taxonomy của datasets, metrics, methodologies; xác định strengths, limitations, unexplored territories cho hướng LLM-on-tabular. Semantic Scholar
Kết quả/Kết luận chính: Tổng hợp gần như đầy đủ các serialization scheme, prompt template, fine-tuning strategy cho LLM trên tabular tính tới 2024.
Hạn chế: Chưa cover sâu phần anomaly detection (chỉ điểm qua); cập nhật tới giữa 2024.
Hướng phát triển: AD-specific frameworks, multi-modal tabular (kết hợp text + số), foundation models cho tabular.

mo_dau.tex (4 fixes):

- "end-to-end hoàn chỉnh" → "đầu cuối hoàn chỉnh"
- "kiểm tra end-to-end" → "kiểm tra đầu cuối"
- "đánh giá end-to-end" → "đánh giá đầu cuối"
- "customizable enterprise data-quality rules" → "luật kiểm soát chất lượng dữ liệu doanh nghiệp"
- "archetype dữ liệu" → "kiểu dữ liệu nguyên mẫu" (×2)

chuong1.tex (1 fix):

- "archetype dữ liệu" → "kiểu dữ liệu nguyên mẫu"

chuong2.tex (3 fixes):

- "System prompt:" → "Lời nhắc hệ thống (system prompt):"
- "Structured output:" → "Đầu ra có cấu trúc (structured output):"
- "streaming UI" → "giao diện truyền phát (streaming UI)"

chuong3.tex (12 fixes):

- "chronological split theo năm" → "chia theo thứ tự thời gian (theo năm)"
- "customizable enterprise data-quality rules" → "luật kiểm soát chất lượng dữ liệu doanh nghiệp"
- "Auto-detect encoding" → "Tự động phát hiện mã hoá"
- Pipeline stage names: Schema Normalization, Merging & Enrichment, Windowing Time-Series, Synthetic Augmentation, Feature Selection & Scaling → added Vietnamese
  translations first
- "Subset/Padding/Rename/Combo variants" → "Biến thể tập con/độn thêm cột/đổi tên cột/kết hợp"
- "kiến trúc end-to-end ghép" → "kiến trúc đầu cuối ghép"
- "pipeline end-to-end" → "pipeline đầu cuối" (×2, incl. figure caption)
- "Upload & Validation" → "Tải lên và xác thực (Upload & Validation)"
- "data archetype" / "archetype dữ liệu" → "kiểu dữ liệu nguyên mẫu"
- "WebSocket listener" → "Trình lắng nghe WebSocket"

chuong4.tex (7 fixes):

- "hệ thống end-to-end" → "hệ thống đầu cuối"
- "sanity check" (×2) → "kiểm tra cơ bản"
- "Format Compliance" (header + bullet) → "Tuân thủ định dạng"
- "out-of-template stress test" → "kiểm tra ứng suất ngoài mẫu"
- "Đánh giá hệ thống end-to-end" → "Đánh giá hệ thống đầu cuối"
- "Latency end-to-end" → "Độ trễ đầu cuối"
- "out-of-the-box" → "với cấu hình mặc định mà không cần tùy chỉnh thêm"
- "latency end-to-end p50" → "độ trễ đầu cuối p50"

chuong5.tex (5 fixes):

- "Latency end-to-end" → "Độ trễ đầu cuối"
- "Self-supervised representation learning" + "contrastive learning" → Vietnamese first
- "Multi-modal anomaly detection" → Vietnamese first
- "Active learning / Human-in-the-loop" → Vietnamese first
- "LLM local" + "latency" → "LLM cục bộ" + "độ trễ"
