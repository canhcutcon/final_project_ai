# Story 6.1: App Shell & Design System Setup

## Status

Approved

## Story

**As a** developer,
**I want** to scaffold the Next.js 14 App Router project with shared layout components (Sidebar, TopBar) and the Sovereign Intelligence design system tokens,
**so that** all pages share consistent navigation, typography, color tokens, and glassmorphism styling.

## Acceptance Criteria

1: Khởi tạo project Next.js 14 (App Router, TypeScript, TailwindCSS) tại `{frontend}/` với cấu trúc `app/`, `components/`, `lib/`, `hooks/`.
2: Cấu hình Tailwind config với toàn bộ **color tokens** theo Design System (surface, primary, secondary, error, tertiary, outline-variant, on-surface, ...) — sử dụng chính xác hex values từ design.
3: Cấu hình font family **Inter** (wght 100-900) qua Google Fonts, áp dụng `font-headline`, `font-body`, `font-label` aliases.
4: Xây dựng component `SideNavBar` (fixed left, width 264px):

- Logo block "Sovereign Intelligence" (font-black, uppercase, tracking-tighter) + tagline "Intelligence Framework v1.0" (10px, tracking 0.2em)
- 4 nav items chính: Dashboard (dashboard icon), Upload (cloud_upload), Analyses (analytics), Reports (description) — dùng Material Symbols Outlined
- Active tab: bg-white, text-indigo-700, shadow-sm, rounded-lg; icon filled (FILL 1)
- Inactive tabs: text-slate-500, hover: text-indigo-600, hover bg-slate-200/50
- Bottom section (border-t): Settings + Support links
- Font: 13px, font-medium, tracking-wide, uppercase
  5: Xây dựng component `TopAppBar` (fixed top, left-64, glassmorphism):
- Background: `bg-slate-50/85 backdrop-blur-md`, height 64px
- Left: Search input (rounded-full, pl-10, bg-slate-200/40, placeholder "Search parameters...")
- Right: Export PDF button (primary style), Notification icon, Help icon, User avatar (rounded-full, 32px)
  6: Tạo `RootLayout` component tích hợp SideNavBar + TopAppBar, main content area `ml-64 pt-20 p-12 min-h-screen`.
  7: Implement CSS utilities: `.glass-panel` (rgba 80% + blur 12px), `.signature-gradient` (linear-gradient 135deg), `.no-scrollbar`.
  8: Setup API client service (`lib/api.ts`) với base URL cấu hình qua environment variable, chuẩn bị cho các API calls từ Epics 1-5.

## Tasks / Subtasks

- [ ] Task 1: Scaffold Next.js 14 project (AC: 1)
  - [ ] Run `npx create-next-app@14` with App Router, TypeScript, TailwindCSS, ESLint at `{frontend}/`
  - [ ] Create folder structure: `app/` (pages), `components/` (shared UI), `components/layout/` (shell), `lib/` (utilities), `hooks/` (custom hooks), `styles/` (global CSS), `types/` (TypeScript interfaces)
  - [ ] Create `app/(pages)/dashboard/page.tsx`, `app/(pages)/upload/page.tsx`, `app/(pages)/analyses/page.tsx`, `app/(pages)/reports/page.tsx` as stub pages
  - [ ] Setup `.env.local` with `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1`

- [ ] Task 2: Configure Tailwind design tokens (AC: 2)
  - [ ] Extend `tailwind.config.ts` `theme.extend.colors` with all Sovereign Intelligence tokens: `surface: "#f7f9fc"`, `surface-container-low: "#f2f4f7"`, `surface-container-lowest: "#ffffff"`, `surface-container-high: "#e6e8eb"`, `surface-container-highest: "#e0e3e6"`, `primary: "#24389c"`, `primary-container: "#3f51b5"`, `primary-fixed: "#dee0ff"`, `primary-fixed-dim: "#bac3ff"`, `secondary: "#006a6a"`, `secondary-container: "#90efef"`, `error: "#ba1a1a"`, `error-container: "#ffdad6"`, `tertiary: "#573f00"`, `tertiary-fixed-dim: "#fbbc00"`, `on-surface: "#191c1e"`, `on-surface-variant: "#454652"`, `on-primary: "#ffffff"`, `on-secondary-container: "#006e6e"`, `on-error-container: "#93000a"`, `outline: "#757684"`, `outline-variant: "#c5c5d4"`, `inverse-surface: "#2d3133"`, `inverse-on-surface: "#eff1f4"`, `background: "#f7f9fc"`
  - [ ] Extend `theme.extend.fontFamily` with `headline: ["Inter"]`, `body: ["Inter"]`, `label: ["Inter"]`
  - [ ] Extend `theme.extend.borderRadius` with `DEFAULT: "0.125rem"`, `lg: "0.25rem"`, `xl: "0.5rem"`, `full: "0.75rem"`

- [ ] Task 3: Configure Inter font (AC: 3)
  - [ ] Add Google Fonts `Inter` (wght 100-900) import via `next/font/google` in root layout
  - [ ] Apply font to `<body>` with `font-body` class
  - [ ] Verify `font-headline`, `font-body`, `font-label` aliases resolve correctly

- [ ] Task 4: Build SideNavBar component (AC: 4)
  - [ ] Create `{frontend}/components/layout/SideNavBar.tsx`
  - [ ] Implement logo block: "Sovereign Intelligence" `text-lg font-black uppercase tracking-tighter` + tagline `text-[10px] font-bold uppercase tracking-[0.2em]`
  - [ ] Add Material Symbols Outlined link in `app/layout.tsx` head: `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1`
  - [ ] Implement nav items array with `label`, `icon`, `href` — map to links with active state detection via `usePathname()`
  - [ ] Active tab styling: `text-indigo-700 bg-white shadow-sm rounded-lg`, icon `style={{ fontVariationSettings: "'FILL' 1" }}`
  - [ ] Inactive tab styling: `text-slate-500 hover:text-indigo-600 hover:bg-slate-200/50`
  - [ ] All nav text: `text-[13px] font-medium tracking-wide uppercase`
  - [ ] Bottom section: `border-t border-slate-200` with Settings (settings icon) + Support (contact_support icon)
  - [ ] Container: `h-screen w-64 fixed left-0 top-0 z-40 bg-slate-100 flex flex-col p-4 space-y-2`

- [ ] Task 5: Build TopAppBar component (AC: 5)
  - [ ] Create `{frontend}/components/layout/TopAppBar.tsx`
  - [ ] Container: `fixed top-0 right-0 left-64 h-16 z-50 bg-slate-50/85 backdrop-blur-md flex justify-between items-center px-8`
  - [ ] Left side: Search input with search icon — `pl-10 pr-4 py-1.5 bg-slate-200/40 border-none rounded-full text-sm w-64 focus:ring-2 focus:ring-indigo-500/20`
  - [ ] Right side: Export PDF button (`px-4 py-1.5 text-xs font-bold uppercase tracking-widest text-indigo-700 bg-indigo-100 rounded`), Notification button (notifications icon), Help button (help_outline icon), User avatar (`h-8 w-8 rounded-full`)

- [ ] Task 6: Build RootLayout integrating shell (AC: 6)
  - [ ] Update `{frontend}/app/layout.tsx` to render `<SideNavBar />` + `<TopAppBar />` + `<main className="ml-64 pt-20 p-12 min-h-screen">{children}</main>`
  - [ ] Set `<body className="bg-surface text-on-surface font-body">`
  - [ ] Add `<html lang="en">` with proper meta tags

- [ ] Task 7: Implement CSS utilities (AC: 7)
  - [ ] Add to `{frontend}/styles/globals.css`:
    - `.glass-panel { background: rgba(255, 255, 255, 0.8); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); }`
    - `.signature-gradient { background: linear-gradient(135deg, #24389c 0%, #3f51b5 100%); }`
    - `.no-scrollbar::-webkit-scrollbar { display: none; }`
    - `.material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }`

- [ ] Task 8: Setup API client service (AC: 8)
  - [ ] Create `{frontend}/lib/api.ts` with `const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL`
  - [ ] Implement `apiFetch<T>(endpoint: string, options?: RequestInit): Promise<T>` wrapper with JSON headers, error handling
  - [ ] Create typed API functions stubs: `getDatasets()`, `getAnalyses()`, `getAnalysisResults(id)`, `getReport(id)`, `getPipelineStatus(id)`

- [ ] Task 9: Write tests
  - [ ] Unit test `SideNavBar`: renders all 4 nav items, active state matches current route
  - [ ] Unit test `TopAppBar`: renders search input, export button, icons
  - [ ] Unit test `apiFetch`: handles success and error responses

## Dev Notes

### Relevant Source Tree

```
{frontend}/                              # Currently EMPTY — scaffold here
├── app/                                 # Next.js App Router pages
│   ├── layout.tsx                       # Root layout (SideNavBar + TopAppBar + main)
│   ├── (pages)/
│   │   ├── dashboard/page.tsx           # Dashboard route
│   │   ├── upload/page.tsx              # Upload route
│   │   ├── analyses/page.tsx            # Analyses route
│   │   └── reports/page.tsx             # Reports route
├── components/
│   └── layout/
│       ├── SideNavBar.tsx               # Fixed sidebar navigation
│       └── TopAppBar.tsx                # Fixed top bar with glassmorphism
├── lib/
│   └── api.ts                           # API client service
├── hooks/                               # Custom React hooks (future stories)
├── styles/
│   └── globals.css                      # Global CSS + utilities
├── types/                               # TypeScript interfaces
└── tailwind.config.ts                   # Design token configuration
```

### Patterns & Conventions

- **Frontend is new**: `{frontend}/` is currently empty. This story bootstraps the entire frontend codebase.
- **No backend service layer yet**: `{backend}/` is also empty. API client stubs will be created, but actual endpoints are from Epics 1-5 (FastAPI).
- **Design system reference**: HTML prototypes at `{frontend}/UI/*/code.html` serve as pixel-perfect implementation guides.
- **"No-Line" Rule**: No 1px solid borders for layout sectioning. Use background color shifts and tonal layering only.
- **Component naming**: PascalCase for components (`SideNavBar.tsx`), camelCase for utilities (`api.ts`).
- **Route detection**: Use `usePathname()` from `next/navigation` to determine active sidebar tab.

### Tech Stack

| Technology                | Version | Purpose                                 |
| ------------------------- | ------- | --------------------------------------- |
| Next.js                   | 14.x    | App Router, SSR, TypeScript             |
| React                     | 18.x    | UI components                           |
| TypeScript                | 5.x     | Type safety                             |
| TailwindCSS               | 3.x     | Utility-first CSS, design tokens        |
| Material Symbols Outlined | latest  | Icon library (Google Fonts CDN)         |
| Inter                     | latest  | Typography (Google Fonts via next/font) |

### Testing

- **Framework**: Jest + React Testing Library (installed with create-next-app)
- **Test file location**: `{frontend}/__tests__/` or colocated `*.test.tsx`
- **Test types**: Unit tests for components and utilities
- **Pattern**: Render component → assert DOM output → test interactions

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-03-30 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
