# Story 3.3: ML Anomaly Detection Models (Layer 3)

## Status

Approved

## Implementation Location

**All models are implemented in a single notebook** at `csv_agent_platform/detection/notebooks/` (v10, continuing from v9).

**Workflow**: Notebook experimentation → achieve target metrics → then migrate to `csv_agent_services` production code.

Story 3.3 is implemented **entirely** in `csv_agent_platform/detection/`. Migration to `csv_agent_services` is a **separate follow-up** and only happens after models surpass target results.

## Story

**As a** data analyst,
**I want** ML models to detect anomalies that deterministic rules cannot cover,
**so that** subtle and complex patterns (regional outliers, rare feature combinations) are caught.

## Acceptance Criteria

1: `csv_agent_platform/detection/notebooks/` implement 5 models in a single v10 notebook (continuing from v9):

- **XGBoost CLEAN** — primary supervised, V9 F1=0.88, 48 features without proxy
- **DAE + Mahalanobis** — unsupervised baseline, latent_dim=16
- **Attention-BiLSTM Autoencoder** — unsupervised, tabular feature sequence
- **BiLSTM Classifier** — semi-supervised, DAE latent + raw features
- **Ensemble stacking** — all models, auto-exclude harmful components
  > After results surpass targets → migrate to `csv_agent_services/src/models/` as separate class files inheriting `BaseAnomalyModel`
  > 2: **Attention-BiLSTM Autoencoder** (NEW — unsupervised):
- Input: tabular features as **feature sequence** `[batch, n_features, 1]` — each feature is 1 timestep, NOT time-series window aggregation (unlike V9 A5 which failed F1=0.498)
- Architecture: BiLSTM encoder (bidirectional, hidden=64, 2 layers) → Multi-head Self-Attention (4 heads) → LSTM decoder → reconstruct features
- Features sorted by semantic groups before feeding: Financial → Business Rule → Frequency → Temporal (meaningful feature ordering)
- Anomaly score = reconstruction error (MSE per row)
- Train on **normal-only data** (same as DAE training strategy)
- Attention weights exportable → contributing features (which features were hardest to reconstruct)
  3: **BiLSTM Classifier** (NEW — semi-supervised):
- Input: concatenate DAE latent representation (16-dim) + raw tabular features (48-dim) = 64-dim, reshape to sequence `[batch, 64, 1]`
- Architecture: BiLSTM encoder (bidirectional, hidden=64, 2 layers) → Attention pooling → FC layers `[128, 64, 1]` → sigmoid
- Train with labels from `is_anomaly` column + `biz_rule_violation_count` (semi-supervised: combine rule-based labels + model pseudo-labels)
- Class weighting: `W_ANOMALY=17.972`, `W_NORMAL=0.514` (from `default.yaml`)
- Output: anomaly probability [0, 1] per row
- Advantage over MLP (A8_SupervisedHead): BiLSTM captures sequential dependencies between latent features
  4: `load_model(weights_path)` loads pretrained weights from `.pt` files
  5: `predict(data: np.ndarray) -> AnomalyScores` returns anomaly scores per row
  6: XGBoost: feature importance per prediction (contributing_features)
  7: Attention-BiLSTM AE: attention weights per prediction (contributing_features from reconstruction difficulty)
  8: Ensemble: combine all model scores — auto-exclude models with negative weights (V9 strategy), nested val split for threshold tuning
  9: `BaseAnomalyModel` interface: `load_model()`, `predict()`, `get_threshold()`, `get_feature_importance()`
  10: Model inference on CPU (GPU optional), target < 30s for 100K rows
  11: Rule scores from Layer 2 can be added to feature set for ML models
  12: Fine-tuning support: `train(dataframe, labels?) -> ModelWeights` for all 4 models — allows retrain on new datasets

## Tasks / Subtasks

### Phase 1: Notebook Experimentation (v10 in `csv_agent_platform/detection/notebooks/`)

- [ ] Task 1: Create v10 notebook with data loading & feature pipeline (AC: 1)
  - [ ] Create `csv_agent_platform/detection/notebooks/train_detection_v10.py` (or `.ipynb`)
  - [ ] Reuse v9 data loading, feature engineering (48 CLEAN features), scaling, and train/val/test split
  - [ ] Ensure reproducibility: set_seed(), config-driven hyperparameters

- [ ] Task 2: Implement XGBoost CLEAN in v10 notebook (AC: 1, 6, 10, 12)
  - [ ] Train XGBoost with 48 CLEAN features (no proxy leakage), stratified CV, early stopping
  - [ ] Extract SHAP or built-in feature importance per prediction
  - [ ] Evaluate: F1, Precision, Recall, AUC-ROC → compare with V9 baseline (F1=0.88)
  - [ ] Save model weights (`.json` or `.pkl`)

- [ ] Task 3: Implement DAE + Mahalanobis in v10 notebook (AC: 1, 10, 12)
  - [ ] Define DAE architecture: encoder → latent_dim=16 → decoder (match V9 a2_dae architecture)
  - [ ] Train on normal-only data, MSE loss
  - [ ] Compute reconstruction error (MSE) + Mahalanobis distance in latent space → combined anomaly score
  - [ ] Expose 16-dim latent representation for BiLSTM Classifier input
  - [ ] Save model weights (`.pt`)

- [ ] Task 4: Implement Attention-BiLSTM Autoencoder in v10 notebook (AC: 2, 7, 10, 12)
  - [ ] Define architecture: BiLSTM encoder (bidirectional, hidden=64, 2 layers) → Multi-head Self-Attention (4 heads) → LSTM decoder
  - [ ] Implement feature ordering: sort by semantic groups (Financial → Business Rule → Frequency → Temporal) → reshape `[batch, n_features, 1]`
  - [ ] Train on normal-only data, MSE loss, with attention regularization
  - [ ] Extract attention weights → map to feature names → identify hardest-to-reconstruct features
  - [ ] Evaluate reconstruction error as anomaly score
  - [ ] Save model weights (`.pt`)

- [ ] Task 5: Implement BiLSTM Classifier in v10 notebook (AC: 3, 10, 12)
  - [ ] Define architecture: BiLSTM encoder (bidirectional, hidden=64, 2 layers) → Attention pooling → FC `[128, 64, 1]` → sigmoid
  - [ ] Input: concatenate DAE latent (16-dim) + raw features (48-dim) = 64-dim → reshape `[batch, 64, 1]`
  - [ ] Train with BCE loss, class weights `W_ANOMALY=17.972`, `W_NORMAL=0.514`, using `is_anomaly` + `biz_rule_violation_count` labels
  - [ ] Output: anomaly probability [0, 1] per row
  - [ ] Save model weights (`.pt`)

- [ ] Task 6: Implement Ensemble stacking in v10 notebook (AC: 8, 10)
  - [ ] Collect scores from all models → Logistic Regression meta-learner
  - [ ] Auto-exclude models with negative LR weights (V9 strategy)
  - [ ] Nested validation split for threshold tuning
  - [ ] Evaluate final ensemble: F1, Precision, Recall, AUC-ROC
  - [ ] Compare all individual models vs ensemble → determine which beat targets

- [ ] Task 7: Implement rule-score feature integration in v10 notebook (AC: 11)
  - [ ] Add optional rule score columns (from Story 3.2 `to_features()`) to input features
  - [ ] XGBoost: add rule features to feature matrix
  - [ ] BiLSTM Classifier: extend input dimension from 64 to 64 + N_rule_features
  - [ ] Evaluate with/without rule features → compare results

- [ ] Task 8: Results validation & v10 result notebook (AC: 1-12)
  - [ ] Create `csv_agent_platform/detection/notebooks/v10_detection_result.ipynb` with all metrics, plots, comparisons
  - [ ] Document per-model results: F1, Precision, Recall, AUC-ROC, inference time
  - [ ] Compare v10 vs v9 results
  - [ ] Mark which models surpass target metrics

### Phase 2: Production Migration (ONLY after Phase 1 targets are met)

> **Gate**: Phase 2 starts only when v10 notebook models surpass target metrics.
> Migration destination: `csv_agent_services` (= `{ai_services_detection}`)

- [ ] Task 9: Define BaseAnomalyModel interface (AC: 9)
  - [ ] Create `{ai_services_detection}/src/models/base_model.py`
  - [ ] Define abstract class `BaseAnomalyModel` with methods: `load_model()`, `predict()`, `get_threshold()`, `get_feature_importance()`, `train()`
  - [ ] Define `AnomalyScores` and `ModelWeights` dataclasses

- [ ] Task 10: Migrate validated models to `{ai_services_detection}/src/models/` (AC: 1, 4, 5, 9)
  - [ ] Extract each validated model from v10 notebook into standalone class files:
    - `xgboost_detector.py`, `dae_detector.py`, `attention_bilstm_ae.py`, `bilstm_classifier.py`, `ensemble_detector.py`
  - [ ] Each class inherits `BaseAnomalyModel`, implements `load_model()`, `predict()`, `get_feature_importance()`, `train()`
  - [ ] Load pretrained weights from v10 (`.pt`, `.json`, `.pkl`)

- [ ] Task 11: Write tests (AC: 1-12)
  - [ ] Create `{ai_services_detection}/tests/test_models.py`
  - [ ] Test interface compliance, output shape, value range, feature importance, ensemble auto-exclusion
  - [ ] Test inference performance: < 30s for 100K rows (CPU)

## Dev Notes

### Relevant Source Tree

```
csv_agent_platform/detection/notebooks/   # ← PRIMARY implementation location (Phase 1)
├── train_detection_v9.py                 # Current version (V9)
├── v9_detection_result.ipynb             # V9 results
├── train_detection_v10.py                # NEW — V10 notebook (all models)
└── v10_detection_result.ipynb            # NEW — V10 results

csv_agent_services/ (= {ai_services_detection})   # ← Phase 2 migration (after targets met)
├── configs/
│   └── default.yaml                 # class_weights, splitting config, feature_selection.top_k
├── models/                          # Saved model artifacts (scalers, selectors)
│   └── v9/                          # Pretrained V9 weights (a2_dae_v9.pt, a8_semisup_v9.pt, XGBoost)
├── src/
│   ├── data/
│   │   ├── dataset.py               # PyTorch Dataset classes (TabularDataset, TimeSeriesDataset, MetaDataset)
│   │   ├── features_tabular.py      # TabularFeatureEngineer — 48 features
│   │   ├── feature_selection.py     # MI + correlation feature selector
│   │   ├── transforms.py            # ScalerManager (Standard, MinMax, Robust)
│   │   └── splitter.py              # Stratified, group-aware train/val/test split
│   ├── models/                      # STUB → populated in Phase 2
│   │   ├── base_model.py            # Phase 2 — BaseAnomalyModel interface
│   │   ├── xgboost_detector.py      # Phase 2 — migrated from v10 notebook
│   │   ├── dae_detector.py          # Phase 2
│   │   ├── attention_bilstm_ae.py   # Phase 2
│   │   ├── bilstm_classifier.py     # Phase 2
│   │   └── ensemble_detector.py     # Phase 2
│   ├── training/                    # STUB → training loops
│   └── utils/
│       ├── helpers.py               # get_logger(), load_config(), set_seed(), get_device()
│       └── io.py                    # save_tensor, save_pickle
├── notebooks/                       # V1-V6 training notebooks (reference)
├── tests/
│   └── test_models.py               # Phase 2 — to be created
```

### Patterns & Conventions

- **Class-based modules**: Each model is a standalone class with `config: dict` in `__init__`
- **Config-driven**: All hyperparameters from `configs/default.yaml` — class_weights, latent_dim, hidden_dim, etc.
- **Device selection**: Use `utils.helpers.get_device()` → CUDA > MPS > CPU
- **Seed management**: Use `utils.helpers.set_seed()` for reproducibility
- **Data flow**: Input is `np.ndarray` (already scaled/selected), output is `AnomalyScores` dataclass
- **Model persistence**: PyTorch models saved as `.pt` via `torch.save()`, XGBoost as `.json`/`.pkl`, scalers as `.pkl` via `joblib`
- **Logging**: `get_logger(__name__)` with `%s` formatting
- **Type hints**: `X | Y` syntax, Google-style docstrings

### Tech Stack

- **PyTorch** >=2.0.0 — DAE, Attention-BiLSTM AE, BiLSTM Classifier architectures
- **XGBoost** — supervised detector (add to `requirements.txt` if not present)
- **scikit-learn** >=1.3.0 — LogisticRegression for ensemble meta-learner, StandardScaler
- **NumPy** >=1.24.0 — array operations, Mahalanobis distance
- **scipy** — Mahalanobis distance calculation (`scipy.spatial.distance.mahalanobis`)
- **SHAP** (optional) — XGBoost feature importance explanation
- **pandas** >=2.0.0 — feature preparation
- **pytest** >=7.4.0

### Testing

- **Phase 1 (notebook)**: Validation is done inline in the v10 notebook — metrics, plots, comparisons with v9
- **Phase 2 (production)**: `{ai_services_detection}/tests/test_models.py` — pytest >=7.4.0
- **Fixtures**: Small synthetic datasets (100 rows, 48 features) for quick model inference/training tests
- **Test types**: Unit tests per model class, integration test for ensemble, performance benchmark
- **Note**: Use small hidden dims and 1-2 epochs for test training to keep tests fast

### Notes from Previous Stories

- Story 3.1 and 3.2 create `src/rules/` module — ML models in this story are independent of rules (can be developed in parallel per epic note)
- Story 3.2 `to_features()` provides rule score features that can optionally augment ML model input (AC: 11)
- Existing `src/data/dataset.py` has `TabularDataset`, `TimeSeriesDataset`, `MetaDataset` PyTorch Dataset classes — model `train()` methods should accept these
- Existing `src/data/transforms.py` has `ScalerManager` — data should be scaled before feeding to models
- V9 pretrained weights in `models/v9/` — `load_model()` must be compatible with these files
- **Critical V9 lessons**: V9 A5 BiLSTM failed (F1=0.498) because it used time-series windows on weekly aggregation → NEW Attention-BiLSTM AE uses raw tabular features as sequence. V9 A8 MLP SupervisedHead → replaced by BiLSTM Classifier for sequential dependency capture

### Notebook Versioning Convention

- All notebooks live in `csv_agent_platform/detection/notebooks/`
- Current: v1 → v9 (v9 is latest: `train_detection_v9.py`, `v9_detection_result.ipynb`)
- **Next: v10** — implements all 5 models from this story in a single notebook
- Naming: `train_detection_v10.py` (training script) + `v10_detection_result.ipynb` (results)
- Only models that **surpass target metrics** in v10 are eligible for migration to `csv_agent_services`

## Change Log

| Date       | Version | Description                                                                                                                            | Author        |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 2026-04-11 | 0.1     | Initial draft                                                                                                                          | story-creator |
| 2026-04-11 | 0.2     | Restructure: implement in v10 notebook at csv_agent_platform/detection, Phase 2 migration to csv_agent_services only after targets met | giang         |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
