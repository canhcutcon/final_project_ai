# Story 3.2: Deterministic fixers + LLM suggestions

## Status

Review

## Story

**As a** data operator,
**I want** suggested fixes for detected issues,
**so that** cleaning is one click instead of manual editing.

## Acceptance Criteria

1: Rule-based fixers run first and are marked `source: rule`: date normalization to the TDM `outputFormat`, numeric coercion (strip currency symbols/thousand separators), whitespace/casing normalization, and category synonym mapping (incl. assignments remembered from Epic 1 confirm); each produces `{row, column_key, proposed_value, confidence}`
2: Issues left unresolved by rules are optionally sent to Gemini via the existing service wrapper — batched, error cells only with minimal row context (NFR1) — returning proposals marked `source: llm` with confidence; when the AI-disabled flag is set, this step is skipped silently
3: Suggestions can be applied one-by-one, all-per-column, or all-per-issue-type; every application writes a `transform_log` entry (original value, new value, actor, timestamp) and re-validates the affected cells
4: Numeric values flagged as outliers (`is_outlier_*`) are excluded from fix suggestions — they are flagged, never altered — with a test proving an extreme price is not "corrected"
5: The UI shows an explicit AI-processing notice the first time LLM suggestions are requested in a session (NFR1)

## Tasks / Subtasks

- [x] Task 1 — `transform_log` table (AC: 3) — **this story is the single canonical definer of the full `transform_log` schema; define ALL columns downstream stories need now, in ONE migration, so Stories 3.3 and 3.4 add NO further `ALTER TABLE`s**
  - [x] Prerequisite note: this migration and Story 3.1's `import_issues` migration should land together or in a coordinated order, both blocked on Epic 1 Story 1.1's Alembic chain repair
  - [x] Subtask 1.1 Add `TransformLog` ORM model (new `{backend}/app/models/transform_log.py`) with the **complete** field set that Stories 3.2 (rule/llm), 3.3 (prompt), 3.4 (undo/redo + lineage replay), and Epic 4 Story 4.1 (manual edits) all require — pinned here once so nobody has to migrate it again:
    - `id` (PK)
    - `dataset_id` (FK), indexed
    - `row` (int), `column_key` (str)
    - `original_value`, `new_value` (Text/JSON — store the exact before/after cell values; for `source="prompt"` these are per-cell so replay = re-apply the logged values, no LLM re-query)
    - `source` (Enum: `rule|llm|prompt|manual` — reserve all four values now even though `manual` has no producer until Epic 4 Story 4.1 and `prompt` none until Story 3.3)
    - `actor` (str — user/system identifier), `applied_at` (datetime)
    - **`operation_group` (str/uuid, nullable, indexed)** — groups the many per-cell rows a single prompt application (Story 3.3) or bulk find-&-replace (Epic 4 Story 4.1 Task 4.5) produces, so Story 3.4 can undo/redo them as ONE atomic unit (satisfies Story 3.3 Subtask 6.1's "undoable as a single unit" requirement without a schema change later)
    - **`replay_payload` (JSON, nullable)** — for `source="llm"`/`"prompt"`, persist the LLM's actual output / the resolved operation plan needed to replay deterministically **without re-querying the model** (directly satisfies NFR4 and Story 3.4 Subtask 3.2; do not store a reference to a since-expired API call)
    - **`status` (Enum: `active|undone`, default `active`) + `undone_at` (datetime, nullable)** — the undo marker Story 3.4 needs (Subtask 1.3); reserved here so 3.4 flips it rather than adding a column
    - Index on `dataset_id` and on `operation_group`, following the `Index(...)` style in `data_fix_log.py`/`review_queue.py`
  - [x] Subtask 1.2 A SINGLE Alembic migration creates this full table (optionally combined with Story 3.1's `import_issues` migration into one revision). **Stories 3.3 and 3.4 must NOT add follow-up `ALTER TABLE` migrations for `transform_log` — every column they need is defined here. If a downstream story finds it needs another column, add it here and treat it as a change to this story, not a new migration in that story, to keep the schema in one place (avoids the fragmented `3.2 creates → 3.3 alters → 3.4 alters` chain that risks `down_revision` conflicts per the migration-ordering note in Epic 1 Story 1.1).**
- [x] Task 2 — Rule-based fixers (AC: 1)
  - [x] Subtask 2.1 New module `{backend}/app/services/cleaning_service.py` (sibling to `validation_service.py`/`mapping_service.py`) implementing deterministic fixers, each returning `{row, column_key, proposed_value, confidence: 1.0, source: "rule"}`:
    - **Date normalization**: parse the raw value with `pd.to_datetime(..., errors="coerce")` and reformat to the TDM column's `outputFormat` (e.g. `strftime`) when parse succeeds
    - **Numeric coercion**: strip currency symbols (`$`, `€`, `£`, etc.), thousand separators (`,`), and whitespace, then attempt `float()`/`int()` per the column's `columnType`
    - **Whitespace/casing normalization**: `.strip()` + configurable case normalization (e.g. title-case for name-like `string` columns, upper-case for `category`/code-like columns) — scope this to obviously-safe cases (leading/trailing whitespace, internal double-spaces) rather than guessing intent for arbitrary text
    - **Category synonym mapping**: match the raw value against the TDM column's `dropdownOptions` using the `category_value_map` captured at Epic 1 Story 1.4's mapping confirm (source-value → target-option assignments) — this is the highest-confidence fixer since it replays a user-confirmed decision, not a guess
  - [x] Subtask 2.2 Fixers only act on issues already recorded in `import_issues` (Story 3.1) — do not re-scan the whole dataset independently; take the issue list as input and return proposals only for issues each fixer can actually address
  - [x] Subtask 2.3 Run fixers in a fixed, documented order (date → numeric → category-synonym → whitespace/casing, or whichever order minimizes double-fixing the same cell) and stop at the first fixer that produces a confident proposal for a given cell
- [x] Task 3 — Outlier exclusion (AC: 4)
  - [x] Subtask 3.1 **No `is_outlier_*` flag or reusable "compute outlier flags" utility currently exists anywhere in this codebase** (verified: no matches for `is_outlier` in `{backend}` or `{ai_services_detection}/src`). The epic's dependency note ("reuses logic from `{ai_services_detection}/src/data`") is aspirational, not a literal existing function to call. The closest existing precedent is the z-score-based deviation math in `{backend}/app/services/ai_service.py::_derive_anomaly_type` (used for post-detection anomaly labeling). For this story, implement a lightweight, explicitly-scoped statistical check — e.g. a per-numeric-column z-score or IQR threshold computed over the cleaned column — purely to gate fix-suggestion eligibility (AC4), **not** to duplicate or replace the ML pipeline's actual anomaly detection (`AIService.detect`), which is a separate, more sophisticated system that runs later
  - [x] Subtask 3.2 Before generating a fix proposal for any numeric cell, check it against this outlier gate; if flagged, skip fix-suggestion generation entirely for that cell (it stays in `import_issues` as flagged, untouched) — this applies to both rule-based fixers (Task 2) and the LLM layer (Task 4)
  - [x] Subtask 3.3 Add a test with a fixture containing one extreme/implausible numeric value (e.g. a price 100x the column's typical range) and assert no fix proposal is ever generated for that cell, from either the rule-based or LLM path
- [x] Task 4 — LLM suggestions via the existing service wrapper (AC: 2, 5)
  - [x] Subtask 4.1 Reuse `{backend}/app/services/gemini_fix_service.py`'s `GeminiFixService` class (or its module-level `suggest_fixes(analysis_id, db)` helper as a structural reference) for issues left unresolved after Task 2's rule-based pass — this is "the existing service wrapper" the epic refers to; it is already used synchronously (no Celery) from `POST /api/v1/report/suggest-fix` in `{backend}/app/api/endpoints/report.py`, and already implements the batching (`BATCH_SIZE`), retry/backoff, and Gemini→Ollama→manual-fallback degradation this story needs — do not build a second LLM-calling implementation
  - [x] Subtask 4.2 **Do not confuse this with the separate, already-shipped "Epic 7" post-detection Gemini agent system** (`{backend}/app/tasks/agent_fix_task.py` + `{ai_services_detection}/src/agent/*`, Celery + Redis-cached) — that system fixes *anomalies* found by the ML pipeline; this story fixes *validation issues* found by Story 3.1's TDM validator, before the ML pipeline ever runs. Keep the two systems and their tables (`review_queue`/`data_fix_log` vs. this story's `import_issues`/`transform_log`) completely separate.
  - [x] Subtask 4.3 Batch calls send **only the error cell's value plus minimal row context** (NFR1 data minimization) — mirror `gemini_fix_service.py`'s existing `_build_prompt()` shape (`row_id, error_fields, errors, context`), not the full row/dataset
  - [x] Subtask 4.4 Skip this step entirely (no error, no partial call) when AI is disabled — the flag is `AI_MAPPING_ENABLED` from Epic 1 Story 1.3 (reused here per that story's "single AI on/off flag" note). **Resolve it via an explicit parameter, not a direct global read: whatever function drives the LLM suggestion pass (e.g. `suggest_llm_fixes(..., ai_enabled: bool | None = None)`) uses `settings.AI_MAPPING_ENABLED` when `ai_enabled is None`, else the passed value.** Interactive callers pass nothing (global default); Story 5.2's unattended execution passes `ai_enabled=False` (or the pipeline's `enable_ai_for_unattended` config) so a per-pipeline override can force deterministic-only cleaning without mutating a process-global flag. Mirror the exact `ai_enabled` convention Story 1.3 Subtask 7.1 establishes for `suggest_mapping`, so both reused functions share one signature shape.
  - [x] Subtask 4.5 Track, per import session (e.g. a flag in wizard-page frontend state, or a lightweight session marker), whether LLM suggestions have been requested yet; on the first request, the API response should include enough signal (e.g. `"ai_notice_required": true` or similar) for the frontend to show the AI-processing notice (AC5) — decide and document whether this is tracked client-side only (simplest — a `sessionStorage`/component-state flag, matching this codebase's no-global-store convention) or needs a server-side marker; client-side is preferred unless cross-tab/session persistence is explicitly required
- [x] Task 5 — Apply suggestions (individually / per-column / per-issue-type) (AC: 3)
  - [x] Subtask 5.1 Add `POST /api/v1/datasets/{id}/fixes/apply` (new endpoint, e.g. `{backend}/app/api/endpoints/cleaning.py`) accepting `{scope: "cell"|"column"|"issue_type", target: {row?, column_key?, issue_type?}}` (or a simpler explicit `{fixes: [{row, column_key, proposed_value}]}` list built client-side from the already-fetched suggestions — prefer this simpler shape since it avoids re-deriving "which suggestions match this column/issue_type" server-side when the client already has that list from the suggestion-fetch response)
  - [x] Subtask 5.2 For each applied fix: write the actual value into the working dataset copy (persisted Parquet/CSV in MinIO, or an in-memory-then-saved approach — follow whatever working-copy mechanism Epic 4's Review Entries grid will read from; if that doesn't exist yet, persist a `_cleaned` object alongside the dataset's existing `_processed.parquet` naming convention seen in `pipeline_tasks.py`/`agent_fix_task.py`), write a `TransformLog` entry (`original_value, new_value, source, actor, applied_at`), and re-validate just the affected cell(s) via Story 3.1's targeted re-validation (`Task 5` of Story 3.1) so `import_issues` reflects the post-fix state
  - [x] Subtask 5.3 Bulk-apply (per-column/per-issue-type) should batch the working-copy write and `TransformLog` inserts rather than looping single-cell writes, for performance parity with Story 3.1's NFR3 concerns
- [x] Task 6 — Frontend: suggestions UI + AI notice (AC: 3, 5)
  - [x] Subtask 6.1 Add API client functions to `{frontend}/lib/api.ts`: `getFixSuggestions(datasetId)`, `applyFixes(datasetId, fixes)`, following existing naming/style
  - [x] Subtask 6.2 Render suggestions inline with each flagged cell (in whatever Review/cleaning UI this epic's later stories or Epic 4 establish — if no such grid exists yet at the time this story is implemented, a minimal per-column suggestion list view is an acceptable interim UI, to be superseded by Epic 4's Review Entries grid) with apply-one / apply-all-in-column / apply-all-of-type controls
  - [x] Subtask 6.3 Show a one-time-per-session AI-processing notice (a simple dismissible banner, consistent with this codebase's lightweight UI patterns — no modal library in use elsewhere for non-blocking notices) the first time LLM suggestions are fetched, per Task 4.5's tracking mechanism
- [x] Task 7 — Tests
  - [x] Subtask 7.1 `{backend}/tests/test_cleaning_service.py`: unit tests per rule-based fixer (date, numeric, whitespace/casing, category-synonym using a seeded `category_value_map`), the outlier-exclusion test (Task 3.3), and an LLM-path test with Gemini mocked (assert minimal-payload batching, assert skip when AI-disabled)
  - [x] Subtask 7.2 Integration test: apply-one, apply-all-per-column, apply-all-per-issue-type each produce correct `TransformLog` entries and trigger re-validation of exactly the affected cells

## Dev Notes

### Relevant Source Tree

- `{backend}/app/services/gemini_fix_service.py` — **the "existing service wrapper" this story must reuse** for LLM suggestions. Already used synchronously (no Celery) via `POST /api/v1/report/suggest-fix` in `{backend}/app/api/endpoints/report.py:189`. Implements `GeminiFixService` (batch size, `MAX_RETRIES`/backoff on 429, Gemini→Ollama(`qwen2.5:3b-instruct`)→manual-fallback degradation) and defensive JSON response parsing (`_parse_response`, confidence clamped to `[0,1]`, no-op/empty suggestions forced to zero confidence). Reuse this class/pattern directly; do not write a second LLM client.
- `{backend}/app/tasks/agent_fix_task.py` + `{backend}/app/api/endpoints/agent.py` + `{backend}/app/api/endpoints/review.py` + `{backend}/app/models/review_queue.py` + `{backend}/app/models/data_fix_log.py` — a **separate, already-shipped "Epic 7" system** for post-detection anomaly fixes (Celery + Redis-cached agent from `{ai_services_detection}/src/agent/*`). Structurally useful as a reference for the apply/reject/override endpoint shape (`review.py`), but its tables and Celery task are for a different problem (fixing ML-flagged anomalies, not import-time validation issues) — keep this story's `import_issues`/`transform_log` tables and endpoints fully separate.
- `{backend}/app/services/validation_service.py` — source of `_is_numeric_col`/`_safe_float` helpers and the existing TDM-adjacent rule-set shapes; Story 3.1's new TDM validator (which this story consumes via `import_issues`) lives here or in a sibling module.
- `{backend}/app/core/config.py` — reuse `AI_MAPPING_ENABLED` (Epic 1 Story 1.3's single AI on/off flag) rather than adding a second flag.
- `{ai_services_detection}/src/data/features_tabular.py` — closest existing statistical precedent (agent-behavioral z-score-style ratio features) for outlier-style computation, though it is feature-engineering for the ML pipeline, not a boolean flag utility — do not import this module directly; it's context for how this codebase already thinks about statistical deviation, not a ready-made function.
- `{backend}/app/services/ai_service.py::_derive_anomaly_type` — existing z-score/deviation idiom (`deviations[col]["z"]`) to follow stylistically for this story's lightweight outlier gate.

### Patterns & Conventions

- **Backend architecture**: new `cleaning_service.py` module, layered the same way as `validation_service.py`/`mapping_service.py` — thin endpoints, logic in `services/`.
- **Graceful degradation**: AI-disabled → skip LLM step silently (no error), matching this codebase's dominant degradation pattern (missing Celery, missing model weights, missing WeasyPrint all degrade rather than fail).
- **Data minimization (NFR1)**: LLM batches must carry only the error cell + minimal row context, never the full row/dataset — follow `gemini_fix_service.py`'s existing `_build_prompt()` payload shape exactly.
- **Confidence semantics**: rule-based fixers are deterministic and should report `confidence: 1.0` (they either apply or they don't); LLM-sourced confidence follows the same `[0,1]` clamping and no-op/empty-value zeroing already implemented in `gemini_fix_service.py::_parse_response` — reuse that logic rather than reimplementing confidence sanitization.
- **Error handling**: `HTTPException(404, ...)` for missing dataset; `HTTPException(500, ...)` after `logger.exception(...)` for unexpected failures — consistent with the rest of the API.
- **Frontend state**: no global store; suggestions and the AI-notice-shown flag live in local component/page state, consistent with the rest of the upload wizard.
- **API response format**: no global envelope; flat JSON objects/arrays for suggestions and apply results, consistent with `report.py`'s `suggest-fix`/`apply-fix` response shapes.

### Tech Stack

- **LLM — External API**: `google-generativeai` (`gemini-2.5-flash-lite`) primary, local Ollama (`qwen2.5:3b-instruct`) fallback — same dual-provider stack as `gemini_fix_service.py` and Epic 1 Story 1.3's mapping LLM layer.
- **Data Processing**: pandas for all fixers (date parsing/formatting, numeric coercion, string normalization, duplicate/category checks).
- **Database**: MySQL via async SQLAlchemy for `transform_log` writes; bulk-insert for multi-cell applies.
- **Testing**: pytest/pytest-mock, mocking the Gemini/Ollama calls exactly as `gemini_fix_service.py`'s own usage would be tested (no real external calls).

### Notes from Previous Stories

- Story 3.1 provides `import_issues` (the input to this story's fixers) and its targeted re-validation capability (reused here after each apply, per Task 5.2).
- Epic 1 Story 1.3 provides `AI_MAPPING_ENABLED`, the single AI on/off flag reused here (Task 4.4) rather than introducing a second flag, and Epic 1 Story 1.4 provides the `category_value_map` this story's category-synonym fixer replays (Task 2.1).
- Epic 1 Story 1.1's Alembic chain repair remains a hard prerequisite for this story's `transform_log` migration.
- This story's outlier-exclusion gate (Task 3) is a **new, narrowly-scoped heuristic**, not a reuse of an existing named utility — be explicit in code comments that it is separate from `AIService.detect`'s full ML anomaly pipeline, to avoid future confusion between "flagged during cleaning" and "flagged by the detection model."

### Testing

- Test file location: `{backend}/tests/test_cleaning_service.py` (new), alongside `test_tdm_validation.py` (Story 3.1).
- Framework: pytest + pytest-mock, mocking Gemini/Ollama calls; no real external LLM calls in tests.
- Test types required: per-fixer unit tests, the outlier-exclusion proof test (AC4 — explicitly required, not optional), LLM-path tests (batched/minimized payload, AI-disabled skip), and apply-endpoint integration tests covering all three apply scopes (cell/column/issue_type) with `transform_log` and re-validation assertions.

## Change Log

| Date       | Version | Description                                                     | Author        |
| ---------- | ------- | ----------------------------------------------------------------- | -------------- |
| 2026-07-02 | 0.1     | Initial draft                                                      | story-creator  |
| 2026-07-03 | 1.0     | Implemented deterministic fixers + LLM suggestions + apply/UI      | dev agent      |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (story-developer skill)

### Debug Log References

- Confirmed the Alembic chain (Story 3.1's prerequisite note) was already valid up through `0005_import_issues`; added `0006_transform_log` as the new head. Verified via `ScriptDirectory.walk_revisions()` — chain resolves `0001_baseline → 0002_tdm → 0003_seed_tdm → 0004_import_structure → 0005_import_issues → 0006_transform_log` with no gaps.
- Initial `compute_outlier_cells` implementation used `pd.to_numeric(..., errors="coerce")` directly, which silently dropped currency-symbol values (e.g. `"$999999"`) as NaN instead of flagging them as outliers — caught by 3 failing unit tests (`test_outlier_price_flagged_by_gate` and its dependents). Fixed by extracting a shared `_parse_numeric_string` helper (used by both the outlier gate and `_fix_numeric`) that strips currency/thousands-separators before the numeric parse, so both paths agree on what a cell's "numeric value" is.
- Hit the same macOS AppleDouble resource-fork artifact issue as Story 3.1 (`._*` sibling files auto-created by this external volume) — this time it crashed the frontend Jest/SWC worker (Rust panic on garbage bytes in `._CleaningSuggestionsPanel.tsx`/`._api.test.ts`), not just Alembic's migration discovery. Deleted all `._*` files under `csv_agent_services/` before each test run; not a code defect.
- Discovered `jest.spyOn(api, "<namedExport>")` fails with `TypeError: Cannot redefine property` in this frontend's Jest/SWC/Next.js interop for ANY named export of `lib/api.ts` — reproduced on the pre-existing, already-passing `suggestMapping` export in `MatchColumnsPanel.test.tsx` (unrelated to this story; confirmed pre-existing by testing on an untouched export). Worked around it in this story's new test file by using `jest.mock("@/lib/api")` (whole-module mock) instead of `jest.spyOn` on individual exports — did not touch `MatchColumnsPanel.test.tsx`, since fixing a pre-existing, unrelated test infra issue is out of this story's scope.
- Backend: ran `./venv/bin/python -m pytest tests/test_cleaning_service.py tests/test_tdm_validation.py -q` (41 passed) and the full suite (`-q`, ~806s): 154 passed, 10 failed, 3 skipped — the 10 failures are the same pre-existing, unrelated ones documented in Story 3.1's Dev Agent Record (corrupted `valid.xlsx`/`valid.xls`/`valid.ods` fixtures, unrelated `test_sheets.py` assertions); confirmed none touch `cleaning_service.py`, `transform_log.py`, `cleaning.py`, or this story's migration.
- Frontend: ran `npx jest` (68 tests): 66 passed, 2 pre-existing failures in `MatchColumnsPanel.test.tsx` (the `jest.spyOn` issue above, unrelated to this story and present before any of my changes). `npx tsc --noEmit` — clean, no type errors.

### Completion Notes

1. **What was accomplished**: Added the canonical `transform_log` table (`app/models/transform_log.py` + migration `0006_transform_log`) with the full column set Stories 3.2/3.3/3.4/Epic 4 Story 4.1 need (`operation_group`, `replay_payload`, `status`/`undone_at` reserved now per the story's explicit "define once" instruction). Implemented `app/services/cleaning_service.py`: four rule-based fixers (date→outputFormat, numeric currency/thousands-separator coercion, category-synonym replay from Epic 1's `category_value_map`, whitespace normalization) run in a fixed order and act only on Story 3.1's persisted `import_issues` (never an independent re-scan); an outlier-exclusion gate (z-score, |z|>3) that excludes numeric cells from BOTH the rule-based and LLM paths (AC4, with a dedicated 100x-typical-range test proving no proposal is ever generated for the outlier cell); LLM suggestions via the existing `GeminiFixService` wrapper (no second LLM client built), sending only the flagged cell's value + issue type per row (NFR1), skipped silently when AI is disabled via the `ai_enabled` parameter convention shared with Story 1.3's `suggest_mapping`. Added `POST /datasets/{id}/fixes/suggestions` and `POST /datasets/{id}/fixes/apply` (thin endpoints in `app/api/endpoints/cleaning.py`), the latter writing to a new `_cleaned.csv` MinIO working copy (mirroring `pipeline_tasks.py`'s `_processed.parquet` naming convention), batching `TransformLog` inserts, and re-validating exactly the affected cells via Story 3.1's targeted re-validation. Frontend: `getFixSuggestions`/`applyFixes` in `lib/api.ts`, and a new `CleaningSuggestionsPanel` component (interim UI per Subtask 6.2's explicit allowance, since no Epic 4 Review Entries grid exists yet) with apply-one/apply-all-in-column/apply-all-of-issue-type controls and a client-side-only (sessionStorage), once-per-session AI-processing notice banner.
2. **Items marked `[ ]` Not Done**: none — all tasks/subtasks complete.
3. **Design decisions worth flagging**:
   - Subtask 4.5 offered two options for the AI-notice tracking mechanism (server-side response signal vs. client-side only) and explicitly preferred client-side "unless cross-tab/session persistence is explicitly required." Went with client-side: `used_ai` is returned in the suggestions response (so the frontend knows AI was actually used this call) and the "already shown" flag lives in `sessionStorage`, matching this codebase's no-global-store convention. No server-side per-session marker was added.
   - `CleaningSuggestionsPanel` is NOT wired into the existing Upload Wizard page (`app/(pages)/upload/page.tsx`) — that wizard's step 5 ("Validation") exclusively drives the legacy quick-mode `validate_dataframe()`/rule-set engine (`validateDataset()` in `lib/api.ts`), not the TDM-mapped path this story's cleaning suggestions require. Forcing the panel into that step would have been misleading (it would 400 for any dataset without a confirmed TDM mapping, which is every dataset the current wizard flow produces). Left as a standalone, fully tested component per Subtask 6.2's explicit "interim UI, to be superseded by Epic 4's Review Entries grid" allowance — a future story (likely Epic 4) should mount it once a TDM-mapped wizard/grid flow exists.
   - `_fix_numeric` and `compute_outlier_cells` share one `_parse_numeric_string` helper so the outlier gate and the numeric fixer always agree on what a cell's numeric value is (see Debug Log — this was a real bug caught by tests, not a preemptive abstraction).
4. **Technical debt / follow-up**: (a) the working-copy mechanism (`_cleaned.csv`) is explicitly interim, per the story's own Dev Notes — Epic 4's Review Entries grid may define a different canonical mechanism, at which point `tdm_validation_service.load_working_dataframe`/`save_working_dataframe` (added in this story, reused from Story 3.1's endpoints too) should be updated to match rather than duplicated. (b) The same 10 pre-existing, environment-level test failures noted in Story 3.1 remain unresolved (unrelated to this story). (c) The `jest.spyOn`-on-named-exports issue in this frontend's test environment is pre-existing and affects any test file using that pattern (not just this story's) — worth a follow-up ticket to either fix the Jest/SWC config or standardize on `jest.mock` project-wide.
5. **Story is ready for review.**

#### DoD Checklist Summary

1. Requirements Met — [x] all 5 ACs and their tasks implemented.
2. Coding Standards & Project Structure — [x] layered convention followed (thin endpoints, logic in `cleaning_service.py`); lazy imports inside functions for ORM/SQL; `%`-style logging via `get_logger`; [N/A] no linter (flake8/ruff/black) configured for this backend (confirmed, same as Story 3.1); frontend ESLint/tsc both clean.
3. Testing — [x] 18 new backend tests (`test_cleaning_service.py`: 8 rule-based-fixer unit tests, 3 outlier-exclusion tests, 5 LLM-path tests, 3 apply-endpoint integration tests) + 16 new frontend tests (9 `lib/api.ts` client tests, 7 `CleaningSuggestionsPanel` component tests) — all pass.
4. Functionality & Verification — [x] backend integration tests exercise the real FastAPI app + in-memory SQLite DB + an in-memory fake MinIO (no DB/storage mocks bypassing real logic); frontend component tests exercise real render + user interaction (fireEvent) against the real component tree; [N/A] no live browser walkthrough — the panel isn't wired into a page yet (see design decision above), so there's no live page to click through.
5. Story Administration — [x] all tasks marked `[x]`; decisions documented above; Dev Agent Record complete.
6. Dependencies, Build & Configuration — [x] no new dependencies added (reused `pandas`, existing `GeminiFixService`, existing Jest/pytest tooling); migration chain verified (`0001`→`0006`); [N/A] no new env vars.
7. Documentation — [x] module docstrings explain the outlier-gate/ML-detection separation, the `required_with`-style design notes carried from Story 3.1, and the interim working-copy/UI decisions; no user-facing docs to update (the UI isn't user-reachable yet per the design decision above).

### File List

**New:**
- `csv_agent_services/backend/app/models/transform_log.py`
- `csv_agent_services/backend/app/services/cleaning_service.py`
- `csv_agent_services/backend/app/schemas/cleaning.py`
- `csv_agent_services/backend/app/api/endpoints/cleaning.py`
- `csv_agent_services/backend/alembic/versions/0006_add_transform_log.py`
- `csv_agent_services/backend/tests/test_cleaning_service.py`
- `csv_agent_services/fronted/components/upload/CleaningSuggestionsPanel.tsx`
- `csv_agent_services/fronted/__tests__/CleaningSuggestionsPanel.test.tsx`

**Modified:**
- `csv_agent_services/backend/app/models/__init__.py` (registered `TransformLog`)
- `csv_agent_services/backend/app/main.py` (mounted the `cleaning` router)
- `csv_agent_services/backend/app/services/tdm_validation_service.py` (added `load_working_dataframe`/`save_working_dataframe` working-copy helpers, used by both this story and Story 3.1's validate endpoint)
- `csv_agent_services/backend/app/api/endpoints/validate.py` (Story 3.1's validate/validate-cells endpoints now read the working copy so re-validation sees post-fix values)
- `csv_agent_services/fronted/lib/api.ts` (added `getFixSuggestions`/`applyFixes` + supporting types)
- `csv_agent_services/fronted/__tests__/api.test.ts` (added tests for the two new API functions)

## QA Results

_To be filled by QA agent_
