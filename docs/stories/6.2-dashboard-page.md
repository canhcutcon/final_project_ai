# Story 6.2: Dashboard Page

## Status

Approved

## Story

**As a** user,
**I want** a dashboard showing hero metrics, intelligence trends chart, and recent analyses activity stream,
**so that** I have an authoritative overview of my data intelligence operations at a glance.

## Acceptance Criteria

1: **Hero Metrics Header** — Phần header hiển thị:

- Overline: "OPERATIONAL OVERVIEW" (11px, font-bold, primary, tracking 0.3em, uppercase)
- Title: "System Intelligence" (4xl/text-4xl, font-extrabold, tracking-tight)
- Right side metadata: "Last Sync" + "Nodes Active" (10px uppercase labels, giá trị semibold)
  2: **Bento Grid Hero Metrics** (grid-cols-12, gap-6, 3 cards mỗi card col-span-4):
- **Total Datasets** card: `surface-container-lowest`, rounded-xl, p-8, shadow-sm. Label (12px uppercase tracking-widest) với dot indicator `bg-primary`. Số lớn (5xl font-black). Trend indicator (xs font-bold, text-secondary, icon trending_up). Progress bar (h-1, signature-gradient). Background watermark icon (opacity-5, 8xl, scale on hover).
- **Total Reports** card: Tương tự nhưng dot `bg-secondary`, mô tả text thay progress bar.
- **Anomaly Detection** card: Thêm `border-l-4 border-tertiary-fixed-dim`. Dot `bg-tertiary-fixed-dim`. Giá trị hiển thị dạng percentage. Badge "CRITICAL THRESHOLD" (bg-error-container, text-on-error-container, 10px font-bold).
  3: **Intelligence Trends Chart** (col-span-8, bg-surface-container-low, rounded-xl, p-8):
- Header: "Intelligence Trends" (lg font-bold) + subtitle (xs text-on-surface-variant)
- Legend: Primary (dot bg-primary) + Secondary (dot bg-secondary) — 11px uppercase
- Chart area (h-64): Recharts AreaChart with area gradient fill (primary at 10% opacity). Primary trend line (solid, stroke-width 3). Secondary trend line (dashed). Interactive data points.
- Grid lines: outline-variant at 10% opacity
- Tooltip: inverse-surface background, inverse-on-surface text, 10px font-bold
- X-axis labels: Mon-Sun (10px uppercase tracking 0.2em)
  4: **Recent Analyses** sidebar (col-span-4, surface-container-lowest, rounded-xl, shadow-sm, full height):
- Header: "Recent Analyses" (lg font-bold) + "Live system activity stream" (xs)
- 5 analysis items with status badges: Running (bg-secondary-container, animate-pulse, progress bar), Completed (bg-surface-container-high, check_circle icon), Failed (bg-error-container, error icon, glow shadow, "RETRY JOB" link)
- Footer: "View Historical Ledger" button (10px uppercase tracking-widest)
  5: **Info Cards Row** (grid-cols-2, gap-6, below chart):
- "Cluster Health" card: icon trong signature-gradient container, title sm font-bold, description xs, "VIEW MAP VIEW" link
- "Threat Neutralization" card: icon trong bg-secondary-container, tương tự layout
  6: **FAB** (Floating Action Button): fixed bottom-8 right-8, 56px rounded-full, signature-gradient, white add icon (FILL 1), shadow-2xl, hover scale-110, active scale-95.
  7: Fetch dữ liệu từ API: `GET /api/v1/datasets` (count), `GET /api/v1/analysis` (recent 5), aggregate anomaly rate. Loading skeleton states theo design system.

## Tasks / Subtasks

- [ ] Task 1: Build Hero Metrics Header section (AC: 1)
  - [ ] Create `{frontend}/app/(pages)/dashboard/page.tsx` as the main dashboard page component
  - [ ] Implement header layout with `flex justify-between items-end mb-12`
  - [ ] Left: overline `text-[11px] font-bold text-primary tracking-[0.3em] uppercase` + h2 `text-4xl font-extrabold text-on-surface tracking-tight`
  - [ ] Right: "Last Sync" + "Nodes Active" metadata with `text-[10px] uppercase tracking-wider text-on-surface-variant` labels, divider `w-[1px] h-8 bg-outline-variant/20`

- [ ] Task 2: Build Bento Grid Metric Cards (AC: 2)
  - [ ] Create `{frontend}/components/dashboard/MetricCard.tsx` — reusable card with props: `title`, `value`, `trend`, `icon`, `dotColor`, `variant` (progress | description | threshold)
  - [ ] **Total Datasets** card: watermark database icon (opacity-5, 8xl), dot bg-primary, trending_up icon, progress bar `h-1 signature-gradient w-[78%]`, hover scale watermark
  - [ ] **Total Reports** card: watermark description icon, dot bg-secondary, arrow_upward trend, description text instead of progress bar
  - [ ] **Anomaly Detection** card: watermark analytics icon, `border-l-4 border-tertiary-fixed-dim`, dot bg-tertiary-fixed-dim, percentage display (`text-5xl` + `text-2xl` for %), badge `bg-error-container text-on-error-container text-[10px] font-bold`
  - [ ] Layout: `grid grid-cols-12 gap-6 mb-12`, each card `col-span-12 md:col-span-4`

- [ ] Task 3: Build Intelligence Trends Chart (AC: 3)
  - [ ] Install `recharts` package: `npm install recharts`
  - [ ] Create `{frontend}/components/dashboard/TrendsChart.tsx`
  - [ ] Implement Recharts `AreaChart` + `Area` (primary fill gradient 10% opacity) + `Line` (primary solid) + `Line` (secondary dashed)
  - [ ] Style chart: grid lines `stroke={outlineVariant}` at 10% opacity, `CartesianGrid` with `strokeOpacity={0.1}`
  - [ ] Custom tooltip component: `bg-inverse-surface text-inverse-on-surface px-3 py-2 rounded-md text-[10px] font-bold`
  - [ ] Legend with colored dots (not default Recharts legend)
  - [ ] X-axis labels: `text-[10px] font-bold text-on-surface-variant uppercase tracking-[0.2em]`
  - [ ] Container: `bg-surface-container-low rounded-xl p-8`

- [ ] Task 4: Build Recent Analyses List (AC: 4)
  - [ ] Create `{frontend}/components/dashboard/RecentAnalyses.tsx`
  - [ ] Create `{frontend}/components/ui/StatusBadge.tsx` — reusable badge with `status` prop: `running | completed | failed`
    - Running: `bg-secondary-container text-on-secondary-container animate-pulse` + dot `w-1.5 h-1.5 rounded-full bg-secondary`
    - Completed: `bg-surface-container-high text-on-secondary-container` + check_circle icon 12px
    - Failed: `bg-error-container text-on-error-container shadow-[0_0_8px_rgba(186,26,26,0.2)]` + error icon 12px
  - [ ] Analysis item layout: title (sm font-bold), badge, description (11px), optional progress bar for Running
  - [ ] Failed items: "RETRY JOB" link `text-[10px] font-bold text-primary underline`
  - [ ] Footer: "View Historical Ledger" button `text-[10px] font-black uppercase tracking-widest text-on-surface-variant hover:bg-surface-container-high`
  - [ ] Container: `bg-surface-container-lowest rounded-xl shadow-sm h-full flex flex-col`, scrollable body `overflow-y-auto no-scrollbar`

- [ ] Task 5: Build Info Cards Row (AC: 5)
  - [ ] Create `{frontend}/components/dashboard/InfoCard.tsx` — reusable with `title`, `description`, `icon`, `iconBg`, `linkText`, `linkHref`
  - [ ] "Cluster Health": icon container `w-10 h-10 rounded-lg signature-gradient text-white`, hub icon
  - [ ] "Threat Neutralization": icon container `w-10 h-10 rounded-lg bg-secondary-container text-on-secondary-container`, security icon
  - [ ] Card styling: `p-6 rounded-xl bg-surface-container-high/40`
  - [ ] Link: `text-[10px] font-black uppercase text-primary tracking-widest hover:underline`

- [ ] Task 6: Build FAB component (AC: 6)
  - [ ] Create `{frontend}/components/ui/FAB.tsx`
  - [ ] Styling: `fixed bottom-8 right-8 w-14 h-14 rounded-full signature-gradient text-white shadow-2xl flex items-center justify-center hover:scale-110 active:scale-95 transition-transform z-50`
  - [ ] Icon: add (Material Symbols, FILL 1)

- [ ] Task 7: API integration & loading states (AC: 7)
  - [ ] Add API functions in `{frontend}/lib/api.ts`: `getDashboardStats()`, `getRecentAnalyses(limit: number)`
  - [ ] Create TypeScript interfaces in `{frontend}/types/dashboard.ts`: `DashboardStats`, `AnalysisItem`
  - [ ] Implement loading skeleton components matching card/list dimensions
  - [ ] Fetch data via `useEffect` or React Server Components on page load

- [ ] Task 8: Write tests
  - [ ] Unit test `MetricCard`: renders title, value, trend correctly for each variant
  - [ ] Unit test `StatusBadge`: renders correct styling for running/completed/failed
  - [ ] Unit test `RecentAnalyses`: renders 5 items with correct status badges
  - [ ] Unit test `TrendsChart`: renders without crashing with mock data

## Dev Notes

### Relevant Source Tree

```
{frontend}/
├── app/(pages)/dashboard/
│   └── page.tsx                          # Dashboard page (this story)
├── components/
│   ├── dashboard/
│   │   ├── MetricCard.tsx                # Hero metric card (3 variants)
│   │   ├── TrendsChart.tsx               # Intelligence Trends area chart
│   │   ├── RecentAnalyses.tsx            # Recent analyses activity stream
│   │   └── InfoCard.tsx                  # Cluster Health / Threat info card
│   └── ui/
│       ├── StatusBadge.tsx               # Running/Completed/Failed badge
│       └── FAB.tsx                       # Floating action button
├── lib/
│   └── api.ts                            # API client (from Story 6.1)
├── types/
│   └── dashboard.ts                      # TypeScript interfaces
```

### Patterns & Conventions

- **Design reference**: `{frontend}/UI/dashboard/dashboard_code.html` + `dashboard_screen.png` are pixel-perfect guides.
- **"No-Line" Rule**: Dividers between analysis items use `border-surface-container-low` (background color shift), NOT solid borders.
- **Tonal Layering**: Cards on `surface` background use `surface-container-lowest` (white). Charts section uses `surface-container-low`.
- **Typography scale**: Hero numbers = `text-5xl font-black`, card labels = `text-[12px] font-bold uppercase tracking-widest`, metadata = `text-[10px] uppercase tracking-wider`.
- **Status Badge conventions**: Running = animate-pulse, Completed = static check, Failed = glow shadow.
- **Ghost Borders**: Any separators use `outline-variant` at very low opacity (10-20%).

### Tech Stack

| Technology       | Version | Purpose                                        |
| ---------------- | ------- | ---------------------------------------------- |
| Recharts         | latest  | Area chart, line chart, tooltip                |
| Next.js 14       | 14.x    | App Router, page component                     |
| TailwindCSS      | 3.x     | All styling via utility classes                |
| Material Symbols | latest  | Icons (dashboard, database, description, etc.) |

### Notes from Previous Stories

- Story 6.1 provides: `SideNavBar`, `TopAppBar`, `RootLayout`, Tailwind design tokens, `api.ts` client, CSS utilities (`.signature-gradient`, `.glass-panel`, `.no-scrollbar`).

### Testing

- **Framework**: Jest + React Testing Library
- **Test file location**: `{frontend}/__tests__/dashboard/` or colocated
- **Test types**: Unit tests for each component, integration test for full page render
- **Chart testing**: Recharts components — verify render without crash, don't test SVG internals

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-03-30 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
