# Story 1.1: TDM entity, migration & CRUD API

## Status

InProgress

## Story

**As a** data operator,
**I want** to define and manage Target Data Models with typed columns and validations,
**so that** every import has an explicit canonical schema to map into.

## Acceptance Criteria

1: `target_data_models` table created via Alembic migration in `{backend}/alembic/versions/`; column definitions stored as ordered JSON in the Ingestro-compatible shape (`key`, `label`, `columnType`, optional `description`, `outputFormat`, `dropdownOptions` + `isMultiSelect`, `validations`)
2: Writes validate the JSON against the schema: supported `columnType` values are `string | int | float | date | email | phone | category | percentage | currency_code`; supported validations are `required`, `unique`, `regex` (+ `errorMessage`), `required_with`, `required_with_all`; a `category` column without `dropdownOptions` is rejected with a 422 and a field-level message
3: CRUD endpoints `GET/POST/PUT/DELETE /api/v1/tdm` (list, detail, create, update, delete) with pagination on list
4: The 4 existing rule sets in `{backend}/app/services/validation_service.py` (`real-estate-v1`, `commission-v1`, `invoice-v1`, `contact-v1`) are seeded as TDMs; existing `Template` records are data-migrated into TDMs and template endpoints keep responding as thin aliases (no frontend breakage)
5: Unit tests cover schema acceptance/rejection for representative definitions (valid full model, duplicate keys rejected, unknown columnType rejected, category without options rejected)

## 🧭 Migration-ordering registry (this story repairs the chain everything else builds on)

Every migration across Epics 1–5 declares itself "blocked on Story 1.1's Alembic chain repair" and then appends a new revision. Because these epics proceed in parallel, they will all try to be "the next revision after head" and collide on `down_revision`. To prevent that, **this story establishes the single serialized merge order below; each downstream migration must set its `down_revision` to the current `head` at merge time (rebasing if another migration merged first), and land ONE at a time — never two open PRs both appending to the same head.** Canonical order (adjust only by appending, never by inserting mid-chain):

1. **Story 1.1** — `target_data_models` + `dataset_mappings`/`mapping_history` stubs + `datasets.tdm_id`/`import_status` (this story; the new base head)
2. **Story 2.2** — `datasets.import_structure` (single JSON column; Story 2.3 writes into it, no separate migration)
3. **Story 3.1 + 3.2 (combined revision)** — `import_issues` + the **full** `transform_log` schema (Story 3.2 Subtask 1.1 is the single definer; 3.3/3.4 add no `ALTER`s)
4. **Story 4.2** — `datasets.canonical_path` + TDM `block_on_required_errors`
5. **Story 5.1** — `connectors`
6. **Story 5.2** — `pipelines` + `pipeline_executions`
7. **Story 5.3** — `webhooks` + `webhook_deliveries`

Stories that add no schema of their own (1.3, 1.4, 1.5, 2.1, 2.3, 3.3, 3.4, 4.1, 4.3) do **not** appear here — they reuse columns pinned by the stories above. If a story you're implementing isn't listed and thinks it needs a migration, that's a signal to reconcile it into the owning story's revision rather than appending a stray one.

## Tasks / Subtasks

- [ ] Task 1 — Repair the Alembic migration chain (AC: 1)
  - [x] Subtask 1.1 Confirm `{backend}/alembic/versions/` currently has **no `.py` source files**, only stale `__pycache__` bytecode for 11 prior revisions (`76f37a75a753_init_schemas`, `e5f6a7b8c9d0_add_templates_table`, `f6a7b8c9d0e1_add_config_to_templates`, `d4e5f6a7b8c9_add_anomaly_cases_and_rules`, `c3d4e5f6a7b8_add_data_fix_log_and_review_queue`, `b2c3d4e5f6a7_update_pipeline_run_epic5`, `a7b8c9d0e1f2_add_run_metadata_to_analysis_result`, `a1b2c3d4e5f6_add_report_generation_columns`, `5c9bc0aa9a2b_add_dataset_detailed_columns`, `248e25d10c67_add_storage_path_to_dataset`, `085e99bb47a2_add_preprocess_summary_to_dataset`)
  - [x] Subtask 1.2 Regenerate the missing revision files with `alembic revision --autogenerate` against the current ORM models (or hand-write them from the `.pyc` decompiled structure / from the live DB schema if available) so `alembic upgrade head` works before adding a new revision — do this as a prerequisite fix, not part of the TDM feature diff
  - [ ] Subtask 1.3 Verify `alembic upgrade head` runs clean against a fresh MySQL instance before continuing
- [x] Task 2 — Add the `TargetDataModel` ORM model (AC: 1)
  - [x] Subtask 2.1 Create `{backend}/app/models/target_data_model.py` following the `Template` model pattern (`id`, `name` unique, `description`, `columns` JSON, `created_at`, `updated_at`)
  - [x] Subtask 2.2 Register the model in `{backend}/app/models/__init__.py`
  - [x] Subtask 2.3 Generate the new Alembic revision for `target_data_models` (depends on Task 1 being complete)
- [x] Task 3 — Add `dataset_mappings` and `mapping_history` tables + `datasets.tdm_id`/`import_status` (AC: 1, per Epic 1 "New tables introduced here")
  - [x] Subtask 3.1 Add `tdm_id` (nullable FK to `target_data_models.id`) and `import_status` (string, default `"uploaded"`) columns to `{backend}/app/models/dataset.py`
  - [x] Subtask 3.2 Create stub `DatasetMapping` and `MappingHistory` ORM models (used fully in Stories 1.3/1.4) in the same migration so the schema is stable for downstream stories
  - [x] Subtask 3.3 Single combined Alembic revision for Task 2 + Task 3 tables
- [x] Task 4 — Pydantic schema + validation for the TDM column-definition JSON (AC: 2)
  - [x] Subtask 4.1 Create `{backend}/app/schemas/tdm.py` with `TdmColumnValidation` (`required|unique|regex|required_with|required_with_all` + optional `errorMessage`), `TdmColumn` (`key`, `label`, `columnType: Literal["string","int","float","date","email","phone","category","percentage","currency_code"]`, `description`, `outputFormat`, `dropdownOptions`, `isMultiSelect`, `validations`), `TdmCreate`/`TdmUpdate`/`TdmOut`
  - [x] Subtask 4.2 Add a Pydantic model validator on `TdmColumn` that rejects `columnType == "category"` without non-empty `dropdownOptions` — return FastAPI's default 422 shape with a field-level `loc` pointing at the offending column
  - [x] Subtask 4.3 Add a `TdmCreate`/`TdmUpdate` validator that rejects duplicate `key` values across columns
- [x] Task 5 — CRUD endpoints (AC: 3)
  - [x] Subtask 5.1 Create `{backend}/app/api/endpoints/tdm.py` with `router = APIRouter()` following the layered pattern in `templates.py` (thin router, async `Depends(get_db)`, `HTTPException` for 404/409/422)
  - [x] Subtask 5.2 `GET /api/v1/tdm` — list with `limit`/`offset` (or `page`) query params per the project's existing minimal pagination convention (`Query(default=10, ge=1, le=100)`), ordered by `updated_at desc`
  - [x] Subtask 5.3 `GET /api/v1/tdm/{id}` — detail, 404 if missing
  - [x] Subtask 5.4 `POST /api/v1/tdm` — create, 201, unique-name 409 check like `create_template`
  - [x] Subtask 5.5 `PUT /api/v1/tdm/{id}` — update, 404/409 checks like `update_template`
  - [x] Subtask 5.6 `DELETE /api/v1/tdm/{id}` — delete, 404 if missing, 204 on success
  - [x] Subtask 5.7 Mount the router with prefix `/api/v1/tdm` in `{backend}/app/main.py` alongside the existing router registrations
- [x] Task 6 — Seed the 4 rule sets as TDMs + migrate `Template` → TDM (AC: 4)
  - [x] Subtask 6.1 Write a one-off data migration (Alembic `upgrade()` data step, or a `scripts/` one-shot script run during the same migration) that converts each `_RULE_SETS` entry in `{backend}/app/services/validation_service.py` (`real-estate-v1`, `commission-v1`, `invoice-v1`, `contact-v1`) into a `TargetDataModel` row — map `column_configs` types (`numeric`→`float`, `text`→`string`, `categorical`→`category` with `allowed_values` as `dropdownOptions`) into TDM `columns` JSON
  - [x] Subtask 6.2 Write a data migration step that reads all existing `Template` rows and creates a corresponding `TargetDataModel` per template (best-effort column-type inference from `column_configs`)
  - [x] Subtask 6.3 Keep `{backend}/app/api/endpoints/templates.py` endpoints working unchanged (do not delete `Template` model/table/router) — they remain thin aliases per Epic 1 "Additional Notes" until a later cleanup epic
- [x] Task 7 — Tests (AC: 5)
  - [x] Subtask 7.1 Add `{backend}/tests/test_tdm.py`: valid full TDM definition accepted; duplicate `key` rejected (422); unknown `columnType` rejected (422); `category` column without `dropdownOptions` rejected (422) with field-level message
  - [x] Subtask 7.2 Add CRUD integration tests (create → get → update → delete → 404 after delete) using the existing `httpx` + pytest test-client pattern seen in `tests/test_main.py` / `tests/conftest.py`
  - [x] Subtask 7.3 Add a seed-data test asserting the 4 rule-set TDMs exist after migration/seed and that at least one migrated `Template` produces a matching TDM

## Dev Notes

### Relevant Source Tree

- `{backend}/alembic/versions/` — **currently has no `.py` migration sources**, only stale `__pycache__` bytecode for 11 prior revisions; `alembic upgrade head` will not work as-is. This must be repaired (Task 1) before this story's new revision can be added on top of a working chain.
- `{backend}/alembic.ini`, `{backend}/alembic/env.py` — migration config/environment, already wired up correctly.
- `{backend}/app/models/dataset.py` — current `Dataset` ORM: `id, filename, data_type, size, row_count, column_count, columns_info (JSON), preprocess_summary (JSON), storage_path`. Needs `tdm_id` + `import_status` added here.
- `{backend}/app/models/template.py` — model to mirror for `TargetDataModel`: `id, name (unique), description, column_configs (JSON), rule_set_id, config (JSON), created_at, updated_at`.
- `{backend}/app/services/validation_service.py` — static `_RULE_SETS` dict (`real-estate-v1`, `commission-v1`, `invoice-v1`, `contact-v1`), each a `RuleSetOut` with `column_configs` (types: `numeric`, `text`, `categorical` with `allowed_values`) and `rules` (operator-based row checks). Source data for the AC4 seed migration.
- `{backend}/app/api/endpoints/templates.py` — CRUD reference pattern to copy: thin router, `_serialize()` helper, `select(...)` + `scalar_one_or_none()`, 404/409 `HTTPException`, logger calls on mutation.
- `{backend}/app/schemas/validation.py` — contains `TemplateCreate` (existing schema pattern with a `get_extra_config()`/`get_configs_dict()` helper style) to follow for `TdmCreate`.
- `{backend}/app/main.py` — where routers are mounted with prefixes; add the new `tdm` router here.
- `{backend}/app/db/session.py` — `get_db` async dependency, used identically in the new endpoint module.
- `{backend}/tests/` — existing pytest suite (`test_main.py`, `test_database.py`, `test_upload.py`, …) for structural/style reference; add `test_tdm.py` alongside them.

### Patterns & Conventions

- **Architecture**: layered — `api/endpoints` (routing only) → `services`/inline validators (business logic) → `models` (SQLAlchemy) with `schemas` (Pydantic) for request/response typing. Keep the TDM router thin; put JSON-schema validation logic in the Pydantic schema layer (`schemas/tdm.py`), not in the endpoint.
- **DB access mode**: this is an HTTP-endpoint feature → use **async** SQLAlchemy (`AsyncSession`, `Depends(get_db)`), matching `templates.py`, not the sync pattern used inside Celery tasks.
- **Error handling**: raise `fastapi.HTTPException(status_code=..., detail=...)` for known errors (404 not found, 409 duplicate name, 422 schema violation —422 is FastAPI's default for Pydantic validation errors, so prefer a Pydantic validator over a manual raise where possible to get the standard `{"detail": [...]}` shape). Unexpected errors: catch, `logger.exception(...)`, re-raise as `HTTPException(500, detail=str(e))`.
- **Validation**: request bodies are Pydantic `BaseModel`s in `schemas/`; there is no project-wide custom exception hierarchy — rely on Pydantic validators + `HTTPException`, consistent with existing code.
- **API response format**: no global envelope. Success responses are endpoint-shaped plain dicts (e.g. `{"tdm_id": 12, "message": "..."}` style for actions is optional — list/detail should return the flat resource shape like `_serialize(template)` does). List endpoint returns a bare JSON array (or array + minimal `limit` pagination per existing convention — no cursor/total-count envelope exists elsewhere in this codebase, don't invent one). Errors follow FastAPI default `{"detail": "<message>"}`.
- **Naming/style**: Python 3.10+, 4-space indent, type hints (`-> dict[str, Any]`), `snake_case.py` filenames, logging via `get_logger(__name__)` from `{backend}/app/core/logger.py` using `%`-style lazy formatting, never f-strings inside `logger.*` calls.
- **Import ordering**: `__future__` first (if used), then stdlib, then third-party, then `app.*` local imports.
- No authentication is enforced on any existing route (JWT settings exist in `app/core/config.py` but no auth dependency is applied) — do **not** add auth to the TDM routes; stay consistent with the rest of the API.

### Tech Stack

- **Backend Framework**: FastAPI >=0.110.0 (async, `Depends`), Uvicorn[standard] for serving.
- **Validation**: Pydantic v2 / pydantic-settings >=2.2.1 — use `model_validator`/`field_validator` (v2 API) for the category/dropdownOptions and duplicate-key checks.
- **Database**: MySQL 8.0.35 via SQLAlchemy >=2.0.29, async driver `aiomysql` for the endpoint layer.
- **Migrations**: Alembic >=1.13.1 — chain is currently broken (no committed `.py` sources under `versions/`, only stale bytecode for 11 revisions); repairing it is a prerequisite of this story, not optional groundwork.
- **Testing**: pytest / pytest-mock >=8.1.1, httpx >=0.27.0 as the FastAPI test client — add `test_tdm.py` under `{backend}/tests/`.

### Testing

- Test file location: `{backend}/tests/test_tdm.py` (mirrors existing `test_upload.py`, `test_database.py` naming).
- Framework: `pytest` with `httpx` as the async test client (see `{backend}/tests/conftest.py` for fixture setup) and `pytest-mock` where needed.
- Test types required: unit tests for the Pydantic schema validators (valid model accepted; duplicate key rejected; unknown `columnType` rejected; `category` without `dropdownOptions` rejected — all as 422s with field-level detail) **and** integration tests hitting the CRUD endpoints end-to-end (create → list → get → update → delete → 404-after-delete), plus a seed/migration assertion test that the 4 rule-set TDMs and migrated `Template` rows exist.
- Run with the project's existing `pytest.ini` config (`cd {backend} && pytest`).

## Change Log

| Date       | Version | Description                                                        | Author        |
| ---------- | ------- | ------------------------------------------------------------------ | ------------- |
| 2026-07-02 | 0.1     | Initial draft                                                      | story-creator |
| 2026-07-02 | 0.2     | Implemented TDM entity, schemas, CRUD API, migrations, seed, tests | story-developer |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context) — story-developer skill.

### Debug Log References

- Environment assessment: venv interpreter symlinks are all dangling (Homebrew python@3.10/@3.14 and the framework 3.13 are gone); only `/usr/bin/python3` (3.9.6) remains, with **no project deps**. MySQL (DATABASE_URL → `localhost:3307`) is not running and Docker is unavailable.
- Alembic history: `{backend}/alembic/versions/` has **no `.py` sources**, only orphaned `__pycache__` bytecode compiled for Python 3.10/3.13/3.14. With no matching interpreter and no decompiler, the sources are **not faithfully recoverable** (raw-string grep for `down_revision` linkage across the `.pyc` also came back empty).
- Test harness: `{backend}/tests/conftest.py` **stubs `sqlalchemy`/`alembic`/`aiomysql`/`minio` with `MagicMock`**, so the project has no real-DB integration testing today.
- DB-free verification actually run: loaded `app/schemas/tdm.py` + `app/services/tdm_seed.py` standalone under a minimal `python3.9 + pydantic 2.13.4` venv (bypassing the PEP-604 sibling-module import chain that breaks under 3.9) and executed the full unit-tier assertions — **15/15 checks passed**, including the field-level category error `loc = ('columns', 0)` and all seed-conversion mappings.

### Completion Notes

**Key decision — Alembic chain repaired by SQUASH (not history replay).** Because the 11 historical revision sources are unrecoverable in this environment, Task 1.2 was satisfied by hand-writing a single squashed baseline (`0001_baseline`) that reconstructs the entire pre-TDM schema **directly from the current ORM models** (the source of truth), then stacking the TDM revisions on top:
- `0001_baseline_squash.py` (`0001_baseline`, down_revision=None) — all 9 current tables.
- `0002_add_tdm_and_mapping.py` (`0002_tdm`) — `target_data_models`, `dataset_mappings`, `mapping_history`, `datasets.tdm_id`/`import_status`.
- `0003_seed_tdms_from_rulesets_and_templates.py` (`0003_seed_tdm`) — AC4 data migration.

On a **fresh** MySQL, `alembic upgrade head` builds the whole schema (Task 1.3's exact scenario). For an **existing** DB whose schema already matches, run `alembic stamp 0001_baseline` first, then `alembic upgrade head` to apply `0002`/`0003`. This is a reversible choice: proper history can be regenerated via `--autogenerate` once a DB is available.

**What is DONE and verified (DB-free):**
1. `TargetDataModel` ORM + `DatasetMapping`/`MappingHistory` stubs + `datasets.tdm_id`/`import_status`; models registered.
2. `app/schemas/tdm.py` — Pydantic v2 schema with the `columnType` allowlist, category⇒dropdownOptions rule (field-level 422), duplicate-key rule, and regex/`required_with` shape checks. **Verified executing** (8 schema checks pass).
3. `app/api/endpoints/tdm.py` — full CRUD (list w/ limit/offset paging, get/create/update/delete, 201/204/404/409) mirroring `templates.py`; mounted at `/api/v1/tdm` in `main.py`.
4. `app/services/tdm_seed.py` — pure rule-set/template→TDM conversion helpers; **verified executing** (7 seed checks pass). The seed migration and `templates` router/model/table are left intact (AC4 / 6.3).
5. `tests/test_tdm.py` — unit tier (schema + seed conversions) and a DB-gated integration tier.

**Items NOT done — BLOCKED-pending-DB (why story stays `InProgress`, not `Review`):**
- **Subtask 1.3** — cannot verify `alembic upgrade head` against a fresh MySQL (no MySQL, no Docker, no working interpreter with alembic).
- **Seed migration (`0003`) execution** — written but not run against a DB.
- **CRUD + seed integration tests (7.2, 7.3 DB assertions)** — authored but gated behind `TDM_DB_TESTS=1`; the harness stubs SQLAlchemy so they can't run here.
- **Committed `pytest` run** — blocked because there is no functioning project interpreter (the app uses 3.10+ syntax; the only interpreter is 3.9). The unit logic was instead verified via the standalone loader described above.

**To finish verification once a DB/interpreter exists (exact commands):**
```
# 1. Recreate a working venv on Python 3.10+ and install deps
python3.12 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
# 2. Bring up MySQL on 3307 (docker compose up mysql) — DB name `csv_agent`
# 3. Fresh-DB migration check (AC1 / Subtask 1.3)
cd {backend} && alembic upgrade head
# 4. Run the whole suite incl. TDM integration + seed assertions
TDM_DB_TESTS=1 pytest
```

**Technical debt / follow-ups:** (1) regenerate a proper multi-revision Alembic history via `--autogenerate` if the squash is undesirable; (2) delete the stale `__pycache__` bytecode under `alembic/versions/` once the new chain is confirmed; (3) the venv is broken and must be rebuilt on a 3.10+ interpreter.

### File List

**New:**
- `csv_agent_services/backend/app/models/target_data_model.py`
- `csv_agent_services/backend/app/models/dataset_mapping.py`
- `csv_agent_services/backend/app/schemas/tdm.py`
- `csv_agent_services/backend/app/api/endpoints/tdm.py`
- `csv_agent_services/backend/app/services/tdm_seed.py`
- `csv_agent_services/backend/alembic/versions/0001_baseline_squash.py`
- `csv_agent_services/backend/alembic/versions/0002_add_tdm_and_mapping.py`
- `csv_agent_services/backend/alembic/versions/0003_seed_tdms_from_rulesets_and_templates.py`
- `csv_agent_services/backend/tests/test_tdm.py`

**Modified:**
- `csv_agent_services/backend/app/models/__init__.py` (register `TargetDataModel`, `DatasetMapping`, `MappingHistory`)
- `csv_agent_services/backend/app/models/dataset.py` (add `tdm_id`, `import_status`)
- `csv_agent_services/backend/app/main.py` (mount `/api/v1/tdm` router)

## QA Results

_To be filled by QA agent_
