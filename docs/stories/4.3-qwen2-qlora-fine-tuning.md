# Story 4.3: Qwen2-1.5B QLoRA Fine-Tuning (Primary Model)

## Status

Approved

## Story

**As an** ML engineer,
**I want** to fine-tune Qwen2-1.5B with QLoRA on structured anomaly data → report pairs,
**so that** the model generates accurate, professionally-toned reports in Vietnamese and English.

## Acceptance Criteria

1: Training notebook `{ai_services_generation}/notebooks/train_generation_v1.ipynb` — data preparation, baseline prompt experiments (zero-shot, few-shot trên base Qwen2-1.5B)
2: Training notebook `{ai_services_generation}/notebooks/train_generation_v2.ipynb` — Qwen2-1.5B QLoRA fine-tune, hyperparameter search
3: Training script `{ai_services_generation}/src/training/train_qwen_lora.py` — standalone script extracted từ notebook (cho CI/CD và reproducibility)
4: Shared data loader `{ai_services_generation}/src/training/data_loader.py` — load JSONL dataset, train/val/test split
5: Dataset format: JSONL tại `{ai_services_generation}/data/training/` với `Instruction + Input (Enriched JSON) + Output (Markdown Report)` — **KHÔNG train từ raw JSON**
6: Instruction template bao gồm: tone (professional audit), structure (Executive Summary → Key Issues → Cross Analysis → Recommendations), format rules (bullet points, bold, numeric references)
7: LoRA config: `r=32`, `alpha=64`, target modules `q_proj`, `v_proj`, `k_proj`, `o_proj` — tăng r để compensate cho model nhỏ (1.5B)
8: Fine-tuning chạy được trên Colab T4 (4-bit QLoRA, ~4GB VRAM) và local GPU ≥8GB
9: Training data: **≥5,000-10,000 samples** dạng `Instruction + Enriched Input + Expected Markdown` (Qwen2-1.5B converge nhanh)
10: Adapter weights lưu vào `{ai_services_generation}/models/qwen2-1.5b-lora-adapter/`
11: Training logs + metrics lưu vào `{ai_services_generation}/outputs/`
12: Mỗi experiment iteration tạo notebook version mới (v2.1, v2.2, ...) — giống pattern detection (v7→v8→v9→v10→v11)

## Tasks / Subtasks

- [ ] Task 1: Prepare training data pipeline (AC: 4, 5, 9)
  - [ ] Create `{ai_services_generation}/src/training/__init__.py`
  - [ ] Create `{ai_services_generation}/src/training/data_loader.py` with `GenerationDataLoader` class
  - [ ] Implement `load_jsonl(path) -> list[dict]` — read JSONL files from `{ai_services_generation}/data/training/`
  - [ ] Implement `split_dataset(data, train=0.8, val=0.1, test=0.1) -> tuple` — stratified split preserving issue_type distribution
  - [ ] Define JSONL schema: `{"instruction": str, "input": str (Enriched JSON), "output": str (Markdown Report)}`
  - [ ] Create `{ai_services_generation}/data/training/` directory structure
  - [ ] Generate synthetic training samples from `{ai_services_generation}/data/report-sample/` (~45 existing reports) — pair enriched JSON with expected Markdown output
  - [ ] Target: ≥5,000 training samples via data augmentation (paraphrase instructions, vary report sections, language mix vi/en)

- [ ] Task 2: Build baseline experiment notebook v1 (AC: 1)
  - [ ] Create `{ai_services_generation}/notebooks/train_generation_v1.ipynb`
  - [ ] Cell 1: Data loading — load JSONL samples, display statistics (count, avg length, language distribution)
  - [ ] Cell 2: EDA — analyze report-sample/ files for structure patterns, section headers, tone analysis
  - [ ] Cell 3: Zero-shot baseline — load base Qwen2-1.5B, generate reports from enriched JSON without fine-tuning
  - [ ] Cell 4: Few-shot baseline — 3-shot prompting with example reports, measure improvement
  - [ ] Cell 5: Evaluation — compute BLEU/ROUGE on baseline outputs, document quality gaps
  - [ ] Cell 6: Conclusions — identify what fine-tuning needs to improve (tone, structure, Vietnamese accuracy)

- [ ] Task 3: Define instruction template (AC: 6)
  - [ ] Create instruction template with: role definition (professional audit analyst), output structure (Executive Summary → Key Issues → Cross Analysis → Recommendations), format rules (Markdown with bullet points, bold headers, numeric references from input data)
  - [ ] Vietnamese instruction variant: same structure with Vietnamese section headers and tone guidance
  - [ ] English instruction variant: formal consulting tone
  - [ ] Store instruction templates in `{ai_services_generation}/configs/generation_config.yaml` under `instruction_templates` key

- [ ] Task 4: Implement QLoRA fine-tune notebook v2 (AC: 2, 7, 8)
  - [ ] Create `{ai_services_generation}/notebooks/train_generation_v2.ipynb`
  - [ ] Cell 1: Environment setup — install `transformers`, `peft`, `bitsandbytes`, `trl`, `accelerate`
  - [ ] Cell 2: Load Qwen2-1.5B with 4-bit quantization (`BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.float16)`)
  - [ ] Cell 3: Configure LoRA — `LoraConfig(r=32, lora_alpha=64, target_modules=["q_proj", "v_proj", "k_proj", "o_proj"], lora_dropout=0.05, task_type="CAUSAL_LM")`
  - [ ] Cell 4: Prepare dataset — tokenize instruction+input→output pairs using Qwen2 chat template
  - [ ] Cell 5: Training with `SFTTrainer` — `TrainingArguments(per_device_train_batch_size=4, gradient_accumulation_steps=4, num_train_epochs=3, learning_rate=2e-4, fp16=True, warmup_ratio=0.03, lr_scheduler_type="cosine")`
  - [ ] Cell 6: Hyperparameter search — experiment with lr (1e-4, 2e-4, 5e-4), epochs (2-5), batch size (2-8)
  - [ ] Cell 7: Training curves — plot train/val loss, save to `{ai_services_generation}/outputs/`
  - [ ] Cell 8: Save adapter — `model.save_pretrained("{ai_services_generation}/models/qwen2-1.5b-lora-adapter/")`
  - [ ] Verify: VRAM usage ≤4GB on Colab T4

- [ ] Task 5: Extract standalone training script (AC: 3)
  - [ ] Create `{ai_services_generation}/src/training/train_qwen_lora.py`
  - [ ] Extract notebook logic into reproducible CLI script with argparse: `--data_path`, `--output_dir`, `--epochs`, `--lr`, `--batch_size`, `--lora_r`, `--lora_alpha`
  - [ ] Implement `main()` function: load config → load data → load model → configure LoRA → train → save adapter → log metrics
  - [ ] Add seed setting for reproducibility (`set_seed(42)`)
  - [ ] Save training metrics (loss, eval_loss, learning_rate per step) to `{ai_services_generation}/outputs/training_metrics.json`

- [ ] Task 6: Validate adapter and log results (AC: 10, 11, 12)
  - [ ] Verify adapter weights saved at `{ai_services_generation}/models/qwen2-1.5b-lora-adapter/` — check `adapter_config.json` and `adapter_model.safetensors` exist
  - [ ] Run inference on 10 test samples using fine-tuned adapter — compare with baseline
  - [ ] Log training metrics, sample outputs, and comparison to `{ai_services_generation}/outputs/`
  - [ ] Document experiment results in notebook markdown cells for future reference

## Dev Notes

### Relevant Source Tree

```
csv_agent_platform/generation/
├── notebooks/
│   ├── ViT5_Training_Kaggle.ipynb          ← Reference: early ViT5 experiment
│   ├── finetune_small_llms_colab.ipynb     ← Reference: small LLM fine-tune experiments
│   ├── train_focused_models.ipynb          ← Reference: focused model training
│   ├── train_generation_v1.ipynb           ← NEW: Data prep + baseline (Story 4.3)
│   └── train_generation_v2.ipynb           ← NEW: Qwen2-1.5B QLoRA fine-tune (Story 4.3)
├── src/
│   └── training/
│       ├── train_qwen_lora.py              ← NEW: Standalone training script
│       └── data_loader.py                  ← NEW: Shared JSONL loader
├── models/
│   └── qwen2-1.5b-lora-adapter/           ← NEW: Adapter weights output
├── data/
│   ├── report-sample/                      ← ~45 existing sample reports (training data source)
│   └── training/                           ← NEW: JSONL training data
├── configs/
│   └── generation_config.yaml              ← Instruction templates, training hyperparams
└── outputs/                                ← NEW: Training logs, metrics, curves
```

### Patterns & Conventions

- **Notebook versioning**: Each experiment = new notebook file (`train_generation_v1.ipynb`, `v2.ipynb`, etc.) — never overwrite previous versions (follows detection pattern v7→v8→v9→v10→v11)
- **Python naming**: `snake_case` for files/modules, `PascalCase` for classes
- **Type hints**: Use `X | Y` union syntax (Python 3.10+)
- **Docstrings**: Google-style on all public methods
- **Config-driven**: Hyperparameters and instruction templates in `generation_config.yaml`
- **Logging**: `get_logger(name)` with `%s` formatting
- **Reproducibility**: Seed setting, CLI argparse for all training params, metrics JSON output

### Tech Stack

- **Python** >=3.10
- **PyTorch** >=2.0.0 — model training and inference
- **transformers** (Hugging Face) — Qwen2-1.5B model loading, tokenizer
- **peft** — LoRA/QLoRA adapter configuration and training
- **bitsandbytes** — 4-bit quantization (NF4)
- **trl** — `SFTTrainer` for supervised fine-tuning
- **accelerate** — mixed precision training, device management
- **datasets** (Hugging Face) — JSONL dataset loading
- **NumPy** >=1.24.0 — numerical operations
- **PyYAML** >=6.0 — config loading
- **GPU requirement**: Colab T4 (16GB VRAM, ~4GB used with QLoRA 4-bit) or local GPU ≥8GB

### Notes from Previous Stories

- Story 4.1 `AggregationResult` and Story 4.2 `EnrichmentResult` define the input JSON structure — training data `input` field must match `EnrichmentResult.to_dict()` format
- Report-sample files in `data/report-sample/` serve as reference for expected output style and structure

### Testing

- **Test location**: Training notebooks serve as primary validation (visual inspection of outputs, loss curves)
- **Framework**: pytest >=7.4.0 for `data_loader.py` unit tests
- **Test types**: Unit tests for data loader (JSONL parsing, split ratios), manual evaluation for model outputs
- **No automated model quality tests** — evaluation handled in Story 4.5

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-04-12 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
