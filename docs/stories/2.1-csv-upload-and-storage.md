# Story 2.1: CSV Upload & Storage

## Status

Review

## Story

**As a** user,  
**I want** to upload a CSV file through the API,  
**so that** my data is stored securely for analysis.

## Acceptance Criteria

1: Endpoint `POST /api/v1/upload` tiếp nhận file CSV multipart và lưu trực tiếp vào MinIO bucket.
2: Metadata thông tin cơ bản (filename, size, row_count, column_count, columns_info) được hệ thống ghi vào bảng `datasets` trong MySQL.
3: Cung cấp lại `dataset_id` trong phản hồi để sử dụng ở các bước tiếp theo.
4: Trả về lỗi `400 Bad Request` nếu file type không phải CSV hoặc dung lượng vượt quá giới hạn 100MB.

## Tasks / Subtasks

- [x] Task 1: API Endpoint Setup (AC: 1, 4)
  - [x] Implement `POST /api/v1/upload` in `{backend}/app/api/` using `UploadFile` (FastAPI).
  - [x] Add content-type checks guaranteeing CSV constraints.
  - [x] Add filesize validation halting at 100MB `HTTPException(400)`.
- [x] Task 2: MinIO Storage Service (AC: 1)
  - [x] Create `{backend}/app/services/storage.py` binding Python MinIO SDK.
  - [x] Handle `put_object` logic returning the generated MinIO Object path (e.g. UUID filename).
- [x] Task 3: Database Metadata (AC: 2, 3)
  - [x] Using pandas initially on the uploaded stream to read `row_count` and `column_count`.
  - [x] Insert `Dataset` record via SQLAlchemy asynchronous session containing size/path.
  - [x] Return the generated `dataset_id` to the client.

## Dev Notes

### Relevant Source Tree

- `{backend}/app/api/endpoints/upload.py`
- `{backend}/app/services/storage.py`
- `{backend}/app/models/dataset.py`

### Patterns & Conventions

- **Language**: Python 3.10+ (Union types).
- Use `logger.info("... %s", value)` over internal f-strings for centralized backend logs.
- Validation must be done aggressively using `fastapi.HTTPException`.

### Tech Stack

- FastAPI `UploadFile`
- MinIO Python SDK
- Pandas (for quick shape extraction of the CSV)
- SQLAlchemy (MySQL Dialect)

### Testing

- **Location**: `{backend}/tests/test_upload.py`
- **Type**: Endpoint integration tests utilizing mocked `minio.Minio` interceptors and a sample test CSV file from fixtures.

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-03-27 | 0.1     | Initial draft | story-creator |
| 2026-03-30 | 1.0     | Implementation | Antigravity Agent |

## Dev Agent Record

### Agent Model Used

Gemini 2.5 Flash

### Debug Log References

- Updated `Dataset` schema with missing columns and ran alembic.
- Changed config to `mysql+aiomysql://` driver to fix tests.

### Completion Notes

**DoD Summary:**
1. Updated Dataset model (`size`, `row_count`, `column_count`, `columns_info`) and generated DB migration.
2. Created `MinioService` in `app/services/storage.py`.
3. Implemented `upload_csv` endpoint using `pandas` for metadata extraction and SQLAlchemy for async saving. 
4. Added `fastapi`, `minio`, `pandas`, `pytest-mock` to requirements and set up dependency injection in `app/db/session.py`.
5. Created integration tests testing mocked components checking positive and error routes.

Ready for Review.

### File List

- `csv_agent_services/backend/app/models/dataset.py`
- `csv_agent_services/backend/alembic/versions/*_add_dataset_detailed_columns.py`
- `csv_agent_services/backend/app/services/storage.py`
- `csv_agent_services/backend/app/db/session.py`
- `csv_agent_services/backend/app/api/endpoints/upload.py`
- `csv_agent_services/backend/app/main.py`
- `csv_agent_services/backend/tests/test_upload.py`

## QA Results

_To be filled by QA agent_
