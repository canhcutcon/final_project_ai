# Story 4.4: Gemma 4 2B QLoRA Fine-Tuning (Backup Model)

## Status

Approved

## Story

**As an** ML engineer,
**I want** to fine-tune Gemma 4 2B as a backup model,
**so that** the system has an alternative when Qwen2-1.5B underperforms on specific report types.

## Acceptance Criteria

1: Training notebook `{ai_services_generation}/notebooks/train_generation_v3.ipynb` — Gemma 4 2B QLoRA fine-tune
2: Training script `{ai_services_generation}/src/training/train_gemma_lora.py` — reuse data_loader.py và evaluate.py từ Story 4.3
3: Cùng dataset JSONL với Story 4.3 — đảm bảo consistent comparison
4: LoRA config: `r=32`, `alpha=64`, target modules theo Gemma architecture (`q_proj`, `v_proj`, `k_proj`, `o_proj`)
5: Fine-tuning trên Colab T4 (QLoRA 4-bit, ~5GB VRAM) hoặc local GPU ≥8GB
6: Adapter weights lưu vào `{ai_services_generation}/models/gemma4-2b-lora-adapter/`

## Tasks / Subtasks

- [ ] Task 1: Build Gemma 4 2B QLoRA fine-tune notebook v3 (AC: 1, 3, 4, 5)
  - [ ] Create `{ai_services_generation}/notebooks/train_generation_v3.ipynb`
  - [ ] Cell 1: Environment setup — install `transformers`, `peft`, `bitsandbytes`, `trl`, `accelerate` (same as Story 4.3)
  - [ ] Cell 2: Load training data — reuse `data_loader.py` from Story 4.3, same JSONL dataset for consistent comparison
  - [ ] Cell 3: Load Gemma 4 2B with 4-bit quantization (`BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.float16)`)
  - [ ] Cell 4: Configure LoRA — `LoraConfig(r=32, lora_alpha=64, target_modules=["q_proj", "v_proj", "k_proj", "o_proj"], lora_dropout=0.05, task_type="CAUSAL_LM")` — verify target modules match Gemma architecture
  - [ ] Cell 5: Prepare dataset — tokenize instruction+input→output pairs using Gemma chat template (different from Qwen2 template)
  - [ ] Cell 6: Training with `SFTTrainer` — `TrainingArguments(per_device_train_batch_size=2, gradient_accumulation_steps=8, num_train_epochs=3, learning_rate=2e-4, fp16=True, warmup_ratio=0.03, lr_scheduler_type="cosine")`
  - [ ] Cell 7: Training curves — plot train/val loss, save to `{ai_services_generation}/outputs/`
  - [ ] Cell 8: Quick evaluation — run 10 test samples, compare with Qwen2-1.5B outputs side-by-side
  - [ ] Cell 9: Save adapter — `model.save_pretrained("{ai_services_generation}/models/gemma4-2b-lora-adapter/")`
  - [ ] Verify: VRAM usage ≤5GB on Colab T4

- [ ] Task 2: Extract standalone training script (AC: 2)
  - [ ] Create `{ai_services_generation}/src/training/train_gemma_lora.py`
  - [ ] Reuse `data_loader.py` (from Story 4.3) for dataset loading and splitting
  - [ ] Reuse `evaluate.py` (from Story 4.5, stub for now) for metric computation
  - [ ] Extract notebook logic into CLI script with argparse: `--data_path`, `--output_dir`, `--epochs`, `--lr`, `--batch_size`, `--lora_r`, `--lora_alpha`
  - [ ] Key difference from `train_qwen_lora.py`: model ID (Gemma 4 2B), chat template format, batch size (smaller due to larger model)
  - [ ] Add seed setting for reproducibility (`set_seed(42)`)

- [ ] Task 3: Validate adapter output (AC: 6)
  - [ ] Verify adapter weights saved at `{ai_services_generation}/models/gemma4-2b-lora-adapter/` — check `adapter_config.json` and `adapter_model.safetensors` exist
  - [ ] Run inference on 10 test samples using Gemma 4 2B + adapter
  - [ ] Compare output quality (tone, structure, Vietnamese accuracy) with Qwen2-1.5B outputs from Story 4.3
  - [ ] Log comparison notes in notebook markdown cells

## Dev Notes

### Relevant Source Tree

```
csv_agent_platform/generation/
├── notebooks/
│   ├── train_generation_v1.ipynb           ← Story 4.3: Data prep + baseline
│   ├── train_generation_v2.ipynb           ← Story 4.3: Qwen2-1.5B QLoRA
│   └── train_generation_v3.ipynb           ← NEW: Gemma 4 2B QLoRA (this story)
├── src/
│   └── training/
│       ├── data_loader.py                  ← REUSE from Story 4.3 (shared JSONL loader)
│       ├── train_qwen_lora.py              ← Story 4.3: Qwen2 training script
│       ├── train_gemma_lora.py             ← NEW: Gemma 4 training script
│       └── evaluate.py                     ← Story 4.5: Evaluation (stub or import)
├── models/
│   ├── qwen2-1.5b-lora-adapter/           ← Story 4.3: Qwen2 adapter weights
│   └── gemma4-2b-lora-adapter/            ← NEW: Gemma 4 adapter weights
├── data/
│   └── training/                           ← SAME dataset as Story 4.3 (consistent comparison)
└── outputs/                                ← Training logs, metrics, curves
```

### Patterns & Conventions

- **Notebook versioning**: `train_generation_v3.ipynb` — never overwrite v1/v2 from Story 4.3
- **Code reuse**: Maximize reuse of `data_loader.py` and training infrastructure from Story 4.3
- **Python naming**: `snake_case` for files, `PascalCase` for classes
- **Type hints**: `X | Y` union syntax (Python 3.10+)
- **Docstrings**: Google-style on all public methods
- **Config-driven**: Hyperparameters in `generation_config.yaml`
- **Reproducibility**: Same seed (42), same dataset, same split ratios as Story 4.3

### Tech Stack

- **Python** >=3.10
- **PyTorch** >=2.0.0 — model training and inference
- **transformers** (Hugging Face) — Gemma 4 2B model loading, tokenizer
- **peft** — LoRA/QLoRA adapter configuration
- **bitsandbytes** — 4-bit quantization (NF4)
- **trl** — `SFTTrainer` for supervised fine-tuning
- **accelerate** — mixed precision training
- **GPU requirement**: Colab T4 (~5GB VRAM with QLoRA 4-bit) or local GPU ≥8GB

### Notes from Previous Stories

- Story 4.3 `data_loader.py` provides shared data loading — reuse directly, do not duplicate
- Story 4.3 JSONL dataset in `data/training/` — use identical dataset for fair comparison
- Story 4.3 instruction templates in `generation_config.yaml` — reuse same templates
- Gemma 4 2B uses different chat template format than Qwen2-1.5B — tokenization must adapt
- Gemma 4 2B is ~0.5B params larger → expect slightly higher VRAM (~5GB vs ~4GB) and slower training
- Batch size may need reduction (2 vs 4) to fit within Colab T4 VRAM — compensate with higher gradient_accumulation_steps

### Testing

- **Test location**: Notebook v3 serves as primary validation (visual inspection of outputs, loss curves)
- **Framework**: No additional unit tests — reuses `data_loader.py` tests from Story 4.3
- **Test types**: Manual evaluation via notebook cells, formal evaluation deferred to Story 4.5
- **Comparison**: Side-by-side output comparison with Qwen2-1.5B in notebook Cell 8

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-04-12 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
