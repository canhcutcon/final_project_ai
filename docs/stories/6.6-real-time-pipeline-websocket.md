# Story 6.6: Real-time Pipeline & WebSocket Integration

## Status

Approved

## Story

**As a** user,
**I want** to see real-time pipeline progress with step-by-step status updates across all pages,
**so that** I can monitor long-running backend analyses transparently.

## Acceptance Criteria

1: Custom hook `useWebSocket` kết nối tới FastAPI WebSocket endpoint, nhận realtime status events (pipeline step changes, completion, errors).
2: **Pipeline Progress** component tái sử dụng: step progress bar động hiển thị 5 bước Celery (Preprocess → Detect → Fix → Report → PDF). Mỗi step: icon + label, active step highlighted (primary), completed (secondary check), failed (error).
3: **Pipeline Config** form (trước khi chạy): options `auto_fix` (toggle), `language` (English/Vietnamese), `report_style` (Summary/Detailed). Style: inputs dùng surface-container-highest track, focus 2px Ghost Border primary.
4: Tích hợp realtime status vào **Dashboard** (Recent Analyses list: Running items cập nhật progress bar tự động) và **Upload page** (sau khi Initialize Analysis, hiển thị pipeline progress inline).
5: Render raw logs panel (expandable) và error notifications (bg-error-container, error icon) khi Celery worker bị drop/failed. Toast notification style: bg-inverse-surface, text-inverse-on-surface, rounded-xl, shadow-2xl.
6: Gọi `POST /api/v1/pipeline/run` để start pipeline, `GET /api/v1/pipeline/{id}/status` polling fallback nếu WebSocket disconnect.

## Tasks / Subtasks

- [ ] Task 1: Create `useWebSocket` Hook (AC: 1, 6)
  - [ ] Implement `csv_agent_services/fronted/hooks/useWebSocket.ts` to connect to FastAPI endpoint
  - [ ] Add event handlers for step changes, completion, errors, and fallback polling `GET /api/v1/pipeline/{id}/status`
- [ ] Task 2: Build Pipeline Progress Component (AC: 2)
  - [ ] Develop reusable Pipeline Progress stepped bar
  - [ ] Implement states for each step: active, completed, failed
- [ ] Task 3: Develop Pipeline Config Form (AC: 3)
  - [ ] Create form elements for `auto_fix`, `language`, and `report_style`
  - [ ] Restyle using Ghost Border primary and surface-container-highest
- [ ] Task 4: Integrate Status into Pages (AC: 4)
  - [ ] Update Recent Analyses in Dashboard to reflect WebSocket events
  - [ ] Add Pipeline Progress inline to the Upload Page post initialization (via `POST /api/v1/pipeline/run`)
- [ ] Task 5: Implement Error Notifications & Logging (AC: 5)
  - [ ] Add an expandable raw logs panel
  - [ ] Implement toast notification system specifically for Celery drops/failures

## Dev Notes

### Relevant Source Tree

- `csv_agent_services/fronted/hooks/`
- `csv_agent_services/fronted/components/`
- `csv_agent_services/fronted/app/dashboard/` and `csv_agent_services/fronted/app/upload/`

### Patterns & Conventions

- WebSocket state should be managed gracefully, decoupling UI components from socket connection issues (e.g., using Context or stable hooks).
- Notifications must follow tonal layering for errors (bg-error-container).

### Tech Stack

- React 18
- WebSockets API (native browser object or library like `react-use-websocket`)
- TailwindCSS

### Testing

- Mock WebSocket connection events in tests to verify UI progression.
- Test fallback polling mechanism when connection drops.

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-03-30 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
