# Story 1.2: TDM UI (table + JSON views)

## Status

InProgress

## Story

**As a** data operator,
**I want** to view and edit a TDM as a table or as raw JSON,
**so that** I can manage schemas in whichever form is faster.

## Acceptance Criteria

1: TDM list page (name, column count, updated date) and detail page in `{frontend}` with a toggle between table view and raw-JSON view
2: Paste-JSON-to-create: pasting a valid definition array creates the TDM; invalid JSON or schema violations surface inline errors pointing at the offending column/field
3: Table view supports column add/edit/delete/reorder, including validations and dropdown options for `category` columns
4: Copy button exports the full JSON definition; rename via inline edit on the title
5: Jest tests for the JSON-parse/validate reducer and the table-edit state logic

## Tasks / Subtasks

- [x] Task 1 — API client functions (AC: 1, 2, 4)
  - [x] Subtask 1.1 Add `listTdms()`, `getTdm(id)`, `createTdm(payload)`, `updateTdm(id, payload)`, `deleteTdm(id)` named async functions to `{frontend}/lib/api.ts` using the existing `apiFetch<T>()` wrapper, following the same style as `loadTemplates`/`updateTemplate`/`deleteTemplate`
  - [x] Subtask 1.2 Add `Tdm`, `TdmColumn`, `TdmValidation` types to `{frontend}/types/upload.ts` (or a new `types/tdm.ts` if it grows large) mirroring the backend `columnType`/`validations` shape from Story 1.1
- [x] Task 2 — TDM list page (AC: 1)
  - [x] Subtask 2.1 Create `{frontend}/app/(pages)/tdm/page.tsx` ("use client") following the `templates/page.tsx` pattern: local `useState` for `tdms`, `loading`, `error`; `useEffect` fetch-on-mount with a `cancelled` guard; render name / column count / updated date per row
  - [x] Subtask 2.2 Link each row to `{frontend}/app/(pages)/tdm/[id]/page.tsx` (dynamic detail route)
  - [x] Subtask 2.3 Add a "Create TDM" entry point offering both "blank table" and "paste JSON" starting points
  - [x] Subtask 2.4 Add nav entry in `{frontend}/components/layout/SideNavBar.tsx` for the TDM list page
- [x] Task 3 — TDM detail page with table/JSON toggle (AC: 1, 4)
  - [x] Subtask 3.1 Create `{frontend}/app/(pages)/tdm/[id]/page.tsx`: fetch TDM by id, hold a single source-of-truth `columns: TdmColumn[]` state, and a `view: "table" | "json"` toggle that renders either the table editor or a `<textarea>`/code view derived from `JSON.stringify(columns, null, 2)`
  - [x] Subtask 3.2 Inline-editable title (click-to-edit `<input>` swapping with the heading, following the same local-state pattern as other inline edits in the codebase) that calls `updateTdm`
  - [x] Subtask 3.3 "Copy JSON" button using `navigator.clipboard.writeText` on the current `columns` definition
- [x] Task 4 — Paste-JSON-to-create / JSON view editing with validation (AC: 2)
  - [x] Subtask 4.1 Implement a pure reducer/parser function (e.g. `parseAndValidateTdmJson(raw: string): { columns: TdmColumn[] } | { errors: TdmJsonError[] }`) in `{frontend}/lib/` (new file, e.g. `lib/tdmJson.ts`) that: JSON.parses the input, checks it's an array, and re-validates the same rules as the backend (`columnType` allowlist, `category` requires `dropdownOptions`, duplicate `key` rejected) so errors surface client-side before hitting the API
  - [x] Subtask 4.2 Wire parse errors to inline messages anchored to the offending column index/field (e.g. `{index, field, message}[]`), rendered next to the JSON textarea
  - [x] Subtask 4.3 On successful parse, call `createTdm`/`updateTdm`; surface any 422 `detail` from the backend as a fallback inline error if a validation rule drifts from the client copy
- [x] Task 5 — Table view column editor (AC: 3)
  - [x] Subtask 5.1 Build a table editor component (e.g. `{frontend}/components/templates/TdmColumnTable.tsx` or a new `components/tdm/` folder per the feature-based convention) supporting add row, edit cell (key/label/columnType select/description/outputFormat), delete row, and reorder (up/down controls or drag handle)
  - [x] Subtask 5.2 Per-column validations editor: checkboxes/inputs for `required`, `unique`, `regex` (+ `errorMessage` text input), `required_with`/`required_with_all` (multi-select of other column keys)
  - [x] Subtask 5.3 When `columnType === "category"`, render a `dropdownOptions` editor (add/remove option chips) + `isMultiSelect` checkbox; block save client-side if empty, mirroring the backend rule
  - [x] Subtask 5.4 Keep table-edit state and JSON-view state synchronized through the single `columns` state array from Task 3.1 — table edits update `columns` directly; switching to JSON view re-serializes it
- [x] Task 6 — Jest tests (AC: 5)
  - [x] Subtask 6.1 `{frontend}/__tests__/tdmJson.test.ts` — unit tests for `parseAndValidateTdmJson`: valid array parses; invalid JSON syntax returns a parse error; duplicate keys rejected; unknown `columnType` rejected; `category` without `dropdownOptions` rejected, each asserting the error identifies the correct column
  - [x] Subtask 6.2 `{frontend}/__tests__/TdmColumnTable.test.tsx` — component/state tests for add/edit/delete/reorder actions on the table editor, following the RTL patterns in `__tests__/SideNavBar.test.tsx` / `__tests__/TopAppBar.test.tsx`

## Dev Notes

### Relevant Source Tree

- `{frontend}/app/(pages)/templates/page.tsx` — direct structural reference: local `useState` for list/loading/error, `useEffect` fetch-on-mount with a `cancelled` flag, busy-id pattern for row-level async actions (delete), `confirm()` before destructive actions.
- `{frontend}/components/templates/EditTemplateModal.tsx` — reference for an edit-form component receiving a record + save callback.
- `{frontend}/lib/api.ts` — single file holding all backend calls; `apiFetch<T>()` generic wrapper (sets `Content-Type: application/json`, prefixes `NEXT_PUBLIC_API_BASE_URL`, throws `Error("API Error: <status> <statusText>")` on non-OK). Add the new `Tdm*` functions here, not in a new service class.
- `{frontend}/types/upload.ts` — existing shared TS types (`ColumnConfig`, `Template`, etc.) to extend or sit alongside with `Tdm` types.
- `{frontend}/components/layout/SideNavBar.tsx` — add the new nav entry here.
- `{frontend}/app/(pages)/upload/` — existing 4-step wizard with `[id]` dynamic routes elsewhere in the app (`analyses/[id]`, `pipeline/[id]`, `reports/[id]`) — follow the same `[id]/page.tsx` convention for the TDM detail route.
- `{frontend}/__tests__/` — `api.test.ts`, `SideNavBar.test.tsx`, `TopAppBar.test.tsx` existing tests to mirror in structure and setup (`jest.config.ts`, `jest.setup.ts`).

### Patterns & Conventions

- **Component structure**: feature-based under `{frontend}/components/<feature>/`; PascalCase component files matching the exported component name. Small sub-components used only on one page are kept inline in that page file rather than extracted.
- **Pages**: live at `{frontend}/app/(pages)/<route>/page.tsx`; dynamic detail pages use `[id]/page.tsx`. All interactive pages need the `"use client"` directive.
- **Service pattern**: no class-based service layer — all backend access is functional, centralized in `lib/api.ts` as named async functions built on the single `apiFetch<T>` wrapper. Do not introduce a new abstraction (no axios, no service classes) for TDM calls.
- **State management**: no global store (no Redux/Zustand/Context). Use local component state (`useState`/`useCallback`/`useRef`/`useEffect`) with manual `loading`/`error`/`data` flags around each `apiFetch` call, exactly like `templates/page.tsx`.
- **Styling**: Tailwind CSS 3.4 utility classes inline in JSX; use semantic theme tokens already in use (`bg-surface`, `text-on-surface`, `font-body`) rather than raw colors; Material Symbols icon font is already loaded globally in `layout.tsx`.
- **Naming**: 2-space indent, functional components only, named exports for API helpers in `lib/api.ts`, default exports for page/components files.
- **Mock flag pattern**: several `lib/api.ts` functions honor `NEXT_PUBLIC_MOCK_VALIDATE` for UI-only development — not required for TDM endpoints (they're simple CRUD with no ML dependency), but be aware of the pattern if a reviewer asks for it.

### Tech Stack

- Next.js 14.2.35 App Router, React 18, TypeScript ^5 (strict).
- Tailwind CSS ^3.4.1 + PostCSS for styling.
- Fetch API via the `apiFetch` wrapper — no axios, no React Query/SWR.
- Testing: Jest ^30 + Testing Library ^16 + jsdom, configured via `{frontend}/jest.config.ts` / `jest.setup.ts`.
- No form library (react-hook-form/formik) is used anywhere in this codebase — build the table editor with native controlled inputs, consistent with the rest of the app.

### Notes from Previous Stories

- Story 1.1 defines the backend contract this story consumes: `GET/POST/PUT/DELETE /api/v1/tdm`, with the column-definition JSON shape `{key, label, columnType, description?, outputFormat?, dropdownOptions?, isMultiSelect?, validations?}` and 422 responses (FastAPI default `{"detail": [...]}` shape) for schema violations (duplicate `key`, unknown `columnType`, `category` without `dropdownOptions`). The client-side validator in Task 4.1 must mirror these exact rules so the UI can show errors before round-tripping to the API.
- Story 1.1 seeds 4 rule-set TDMs (`real-estate-v1`, `commission-v1`, `invoice-v1`, `contact-v1`) and migrates existing `Template` rows into TDMs — expect these to already appear in the list page once this story is implemented against a migrated backend.

### Testing

- Test file location: `{frontend}/__tests__/`, flat alongside existing tests (`tdmJson.test.ts`, `TdmColumnTable.test.tsx`).
- Framework: Jest + React Testing Library + jsdom, per `jest.config.ts`/`jest.setup.ts`.
- Test types required: unit tests for the pure JSON-parse/validate function (no rendering needed) and component/interaction tests for the table editor's add/edit/delete/reorder state transitions. No e2e test runner is present in this project — do not introduce one for this story.

## Change Log

| Date       | Version | Description                                                    | Author          |
| ---------- | ------- | -------------------------------------------------------------- | --------------- |
| 2026-07-02 | 0.1     | Initial draft                                                  | story-creator   |
| 2026-07-02 | 0.2     | Implemented TDM UI: API client, types, list/detail pages, JSON reducer, table editor, Jest tests | story-developer |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context) — story-developer skill.

### Debug Log References

- Environment: **no Node.js runtime exists on this machine** (searched Homebrew, nvm, volta, fnm, system paths). `node_modules` (492 pkgs) and a local `jest` binary are present, but with no `node` executable, **Jest, `tsc`, and `next build` cannot be run here** — the frontend analogue of Story 1.1's missing DB/interpreter.
- Verification actually performed: the VS Code **TypeScript language server was queried via `mcp__ide__getDiagnostics` for every new and edited file** — all report **zero diagnostics**, i.e. the whole TS/TSX changeset type-checks clean (imports, `@/` aliases, discriminated unions on the parse result, RTL usage in tests). Files checked clean: `types/tdm.ts`, `lib/api.ts`, `lib/tdmJson.ts`, `lib/tdmColumns.ts`, `components/tdm/TdmColumnTable.tsx`, `app/(pages)/tdm/page.tsx`, `app/(pages)/tdm/[id]/page.tsx`, `components/layout/SideNavBar.tsx`, `__tests__/tdmJson.test.ts`, `__tests__/TdmColumnTable.test.tsx`.

### Completion Notes

**Design decisions:**
- Column-mutation logic and JSON parse/validate were extracted into **pure, DOM-free modules** (`lib/tdmColumns.ts`, `lib/tdmJson.ts`) so AC5's "reducer + table-edit state logic" is unit-testable without rendering, and so both the table view and the JSON view drive a single `columns: TdmColumn[]` source of truth (AC3/3.1/5.4).
- `parseAndValidateTdmJson` mirrors the **exact** Story 1.1 backend rules (columnType allowlist, `category ⇒ dropdownOptions`, duplicate-key, regex/`required_with` shape) and returns errors anchored to `{index, field, message}` so the JSON view highlights the offending column (AC2/4.2).
- Create/edit reuse **one** detail page (`/tdm/[id]`); `id === "new"` is create mode and `?mode=json` opens the JSON view first, satisfying the list page's "blank table" vs "paste JSON" entry points (AC1/2.3).
- The `/tdm` nav entry uses the existing `SideNavBar` pattern (label "Data Models", `schema` icon).

**Note on AC4.3 (surface backend 422 `detail`):** the shared `apiFetch` wrapper throws `Error("API Error: <status> <statusText>")` *without* reading the response body, so it cannot expose FastAPI's `detail`. The **client-side validator is the primary guard** (it mirrors the backend and blocks save before the request); on a backend 422 the generic `API Error: 422 …` message is surfaced as the documented fallback. Enriching `apiFetch` to parse error bodies would be a cross-cutting change beyond this story's scope and is noted as follow-up.

**Items NOT done — BLOCKED-pending-Node (why story stays `InProgress`, not `Review`):**
- **Running the Jest suite** (`__tests__/tdmJson.test.ts`, `__tests__/TdmColumnTable.test.tsx`) — authored and type-checked, but there is no `node` to execute `jest`.
- **`next build` / lint / manual UI verification** — also require a Node runtime.

**To finish verification once Node is available (exact commands):**
```
# install Node 20+ (e.g. `brew install node@20`), then:
cd {frontend}
npm test                # runs the TDM Jest suites (jsdom + RTL)
npm run build           # type-check + production build
```

### File List

**New:**
- `csv_agent_services/fronted/types/tdm.ts`
- `csv_agent_services/fronted/lib/tdmJson.ts`
- `csv_agent_services/fronted/lib/tdmColumns.ts`
- `csv_agent_services/fronted/components/tdm/TdmColumnTable.tsx`
- `csv_agent_services/fronted/app/(pages)/tdm/page.tsx`
- `csv_agent_services/fronted/app/(pages)/tdm/[id]/page.tsx`
- `csv_agent_services/fronted/__tests__/tdmJson.test.ts`
- `csv_agent_services/fronted/__tests__/TdmColumnTable.test.tsx`

**Modified:**
- `csv_agent_services/fronted/lib/api.ts` (add `listTdms`/`getTdm`/`createTdm`/`updateTdm`/`deleteTdm`)
- `csv_agent_services/fronted/components/layout/SideNavBar.tsx` (add `/tdm` nav entry)

## QA Results

_To be filled by QA agent_
