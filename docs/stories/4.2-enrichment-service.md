# Story 4.2: Enrichment Service

## Status

Draft

## Story

**As a** developer,
**I want** aggregated anomaly clusters enriched with numerical reasoning signals, priority ranking, and semantic text,
**so that** the LLM can generate accurate reports without performing any calculations.

## Acceptance Criteria

1: `{ai_services_generation}/src/enrichment/enrichment_service.py` implement `EnrichmentService` class
2: `EnrichmentService.enrich(aggregated_data)` thêm cho mỗi cluster: `ratio` (VD: "1.34% of total"), `priority` (1-3), `impact_score` (0-1), `impact` level (High/Medium/Low)
3: Priority ranking dựa trên `impact_score`: score ≥0.8 → priority 1, ≥0.5 → priority 2, còn lại → priority 3
4: Sinh `semantic_text` cho detailed reports: "Price is 45% higher than district average", "Commission is 80% higher than expected"
5: Individual anomaly detail chứa: `risk_level`, `anomaly_score`, `key_findings[]`, `location_context`, `temporal_context`
6: Output JSON đạt chuẩn "Semantic & Enriched" — LLM có thể viết report chỉ bằng cách rewrite
7: Unit tests tại `{ai_services_generation}/tests/test_enrichment.py`

## Tasks / Subtasks

- [ ] Task 1: Create enrichment module structure (AC: 1)
  - [ ] Create `{ai_services_generation}/src/enrichment/__init__.py`
  - [ ] Create `{ai_services_generation}/src/enrichment/enrichment_service.py` with `EnrichmentService` class skeleton
  - [ ] Define typed dataclasses: `EnrichedCluster` (extends AnomalyCluster with `ratio: str`, `priority: int`, `impact_score: float`, `impact: str`, `semantic_text: str`), `AnomalyDetail` (`risk_level: str`, `anomaly_score: float`, `key_findings: list[str]`, `location_context: str`, `temporal_context: str`), `EnrichmentResult` (`clusters: list[EnrichedCluster]`, `context: ContextSummary`, `cross_analysis: list`, `anomaly_details: list[AnomalyDetail]`)

- [ ] Task 2: Implement impact scoring and priority ranking (AC: 2, 3)
  - [ ] Implement `_compute_impact_score(cluster) -> float` — score 0-1 based on: count relative to total, severity of issue_type, affected_ids breadth
  - [ ] Implement `_assign_priority(impact_score) -> int` — score ≥0.8 → priority 1 (High), ≥0.5 → priority 2 (Medium), else → priority 3 (Low)
  - [ ] Implement `_compute_ratio(cluster, context) -> str` — format as human-readable percentage string (e.g., "1.34% of total records")
  - [ ] Implement `_assign_impact_level(impact_score) -> str` — "High" / "Medium" / "Low" mapping
  - [ ] Load scoring thresholds from `{ai_services_generation}/configs/generation_config.yaml` — not hardcoded

- [ ] Task 3: Implement semantic text generation (AC: 4)
  - [ ] Implement `_generate_semantic_text(cluster, context) -> str` — produce human-readable descriptions per cluster
  - [ ] Template-based text generation using issue_type-specific patterns:
    - HIGH_COMMISSION → "Commission is {ratio}% higher than expected threshold"
    - MISSING_TRANSACTION → "{count} transactions missing from expected records"
    - DUPLICATE_ENTRY → "{count} duplicate entries detected across {affected_count} records"
    - Generic fallback for unknown issue_types
  - [ ] Include quantitative references (exact numbers, percentages) — LLM will rewrite, not recalculate

- [ ] Task 4: Implement individual anomaly detail enrichment (AC: 5)
  - [ ] Implement `_enrich_anomaly_detail(anomaly, cluster_context) -> AnomalyDetail`
  - [ ] Assign `risk_level` based on anomaly_score: ≥0.9 → "Critical", ≥0.7 → "High", ≥0.5 → "Medium", else → "Low"
  - [ ] Extract `key_findings` — list of 2-4 concise bullet-point strings describing what's anomalous
  - [ ] Extract `location_context` from anomaly fields (property address, district, project name)
  - [ ] Extract `temporal_context` from anomaly fields (transaction date, submission date, period)

- [ ] Task 5: Implement main enrich method (AC: 2, 6)
  - [ ] Implement `enrich(aggregated_data: dict) -> EnrichmentResult` orchestrating: score → rank → ratio → semantic text → anomaly details
  - [ ] Sort enriched clusters by priority (1 first) then by impact_score (descending)
  - [ ] Validate input is valid AggregationResult structure, raise `ValueError` if not
  - [ ] Output as structured dict via `.to_dict()` — JSON-serializable, "Semantic & Enriched" standard

- [ ] Task 6: Write unit tests (AC: 7)
  - [ ] Create `{ai_services_generation}/tests/test_enrichment.py`
  - [ ] Test impact score computation with known cluster data
  - [ ] Test priority assignment boundaries: score=0.8 → priority 1, score=0.5 → priority 2, score=0.49 → priority 3
  - [ ] Test ratio string formatting
  - [ ] Test semantic text generation for each issue_type
  - [ ] Test anomaly detail enrichment (risk_level assignment, key_findings extraction)
  - [ ] Test full `enrich()` pipeline with realistic aggregated data from Story 4.1
  - [ ] Test cluster sorting: priority 1 clusters appear first in output

## Dev Notes

### Relevant Source Tree

```
csv_agent_platform/generation/
├── src/
│   ├── aggregation/
│   │   └── aggregation_service.py          ← INPUT: AggregationResult from Story 4.1
│   └── enrichment/
│       └── enrichment_service.py           ← NEW: EnrichmentService class
├── tests/
│   └── test_enrichment.py                  ← NEW: Unit tests
├── configs/
│   └── generation_config.yaml              ← Scoring thresholds, priority boundaries
└── data/
    └── report-sample/                      ← ~45 sample reports (reference for semantic text style)
```

### Patterns & Conventions

- **Python naming**: `snake_case` for files/modules, `PascalCase` for classes (e.g., `EnrichmentService`)
- **Type hints**: Use `X | Y` union syntax (Python 3.10+), type all public method signatures
- **Docstrings**: Google-style `"""triple-quote"""` with `Args:` and `Returns:` sections
- **Config-driven**: All thresholds (impact_score boundaries, priority mapping) in `configs/generation_config.yaml`
- **Logging**: Use centralized `get_logger(name)` pattern, `%s` string formatting
- **Data flow**: Input is `AggregationResult.to_dict()` from Story 4.1, output is `EnrichmentResult.to_dict()` for Story 4.6
- **No framework**: Pure Python — follows detection pipeline pattern

### Tech Stack

- **Python** >=3.10 (union type syntax)
- **NumPy** >=1.24.0 — numerical computations for scoring
- **PyYAML** >=6.0 — config loading for thresholds
- **pytest** >=7.4.0 — unit testing

### Notes from Previous Stories

- Story 4.1 `AggregationResult` is the direct input — import dataclasses from `aggregation_service.py`
- Cluster structure: `issue_type`, `count`, `samples[]`, `affected_ids[]` — enrich these fields in-place
- Context summary (`total_records`, `expected`, `missing`, `anomaly_ratio`) available for ratio computation

### Testing

- **Test location**: `{ai_services_generation}/tests/test_enrichment.py`
- **Framework**: pytest >=7.4.0
- **Fixtures**: Extend `conftest.py` with enrichment-specific fixtures (sample aggregated data, expected enriched output)
- **Test types**: Unit tests per method, integration test for full `enrich()` pipeline
- **Boundary testing**: Explicitly test priority threshold boundaries (0.8, 0.5 edge values)

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-04-12 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
