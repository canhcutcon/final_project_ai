# Story 2.3: Data Preprocessing Pipeline

## Status

Review

## Story

**As a** user,  
**I want** my data to be automatically cleaned and preprocessed,  
**so that** it's ready for anomaly detection inference.

## Acceptance Criteria

1: Module `DataService.preprocess()` thực hiện đầy đủ các bước chuẩn hóa cơ bản: xử lý missing values, mã hóa categorical (encoding), căn chỉnh tỉ lệ numeric (scaling).
2: Tạo object `PreprocessResult` chứa summary chi tiết: danh sách cột processed, số hàng bị bỏ đi do lỗi/rỗng, và các thuật toán transform đã được sử dụng.
3: Dữ liệu chuẩn hoá xử lý sau cùng được gán lưu lại MinIO thành một file processed CSV (khác với gốc).

## Tasks / Subtasks

- [x] Task 1: Preprocessing Core Logic (AC: 1)
  - [x] Create wrapper orchestrating logic in `{ai_services_detection}/src/data/cleaning.py` and `{ai_services_detection}/src/data/transforms.py`.
  - [x] Handle missing imputation cleanly.
  - [x] Standardize numbers scaling and encode labels (Ordinal or Nominal strings).
- [x] Task 2: Object Wrapping and Output Transport (AC: 2, 3)
  - [x] Track column alterations returning summarized dictionaries representing `PreprocessResult`.
  - [x] Push the final memory DataFrame payload to MinIO saving using path suffix `_processed.parquet` or CSV.
  - [x] Link Processed status metrics via Database connection.

## Dev Notes

### Relevant Source Tree

- `{backend}/app/services/data_service.py`
- Context relies directly on actual logic inside: `{ai_services_detection}/src/data/cleaning.py` and `SchemaStandardizer`.
- `{backend}/app/services/storage.py`

### Patterns & Conventions

- Maintain single-responsibility where Data Pipelines only yield resulting signals. Let routers control IO returns.
- Save processed sets mostly in `.parquet` natively utilizing `pandas.to_parquet()` leveraging PyArrow.

### Tech Stack

- Pandas 2.0.0+ (PyArrow Backend)
- Scikit-learn (Transformers/Imputers)
- MinIO SDK

### Testing

- **Location**: `{backend}/tests/test_data_services.py`
- **Type**: Create mocked messy `sample_df` dataframes capturing null values and verifying output `PreprocessResult` accurately describes drop thresholds.

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-03-27 | 0.1     | Initial draft | story-creator |
| 2026-03-30 | 1.0     | Implementation| Antigravity Agent |

## Dev Agent Record

### Agent Model Used
Gemini 2.5 Flash

### Debug Log References
- Successfully bound imports from `csv_agent_platform/detection` by dynamically appending to `sys.path`.
- Implemented `LabelEncoder` natively as AC 1 calls for categorical encoding which was omitted in the default `transforms.py`.

### Completion Notes
**DoD Summary:**
1. Completed `DataService.preprocess()` which delegates text/scaling ops to AI detection source logic and adds sklearn validation.
2. Hooked the background execution where outputs stream directly to `Minio` storing `.parquet` tables under `_processed.parquet` key suffix.
3. Created an Alembic migration introducing `preprocess_summary` matching the AC requirements to log actions.
4. Confirmed validation unit tests pass accurately tracing pipeline transformations.

Ready for Review.

### File List
- `csv_agent_services/backend/app/services/data_service.py`
- `csv_agent_services/backend/app/models/dataset.py`
- `csv_agent_services/backend/tests/test_data_services.py`
- `csv_agent_services/backend/requirements.txt`
- `csv_agent_services/backend/alembic/versions/*_add_preprocess_summary_to_dataset.py`

## QA Results

_To be filled by QA agent_
