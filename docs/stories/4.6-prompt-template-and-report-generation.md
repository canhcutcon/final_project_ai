# Story 4.6: Prompt Template & Report Generation Service

## Status

Draft

## Story

**As a** user,
**I want** the system to generate a natural language report explaining the anomalies found,
**so that** I can understand the results without deep technical knowledge.

## Acceptance Criteria

1: Prompt template `{ai_services_generation}/src/templates/report_prompt.j2` nhận Enriched JSON từ Story 4.2, render thành prompt chuẩn với `### Instruction` + `### Input` + `### Output`
2: Fallback template `{ai_services_generation}/src/templates/fallback_report.j2` — Jinja2 template sinh Markdown từ Enriched data (không cần LLM)
3: `{ai_services_generation}/src/inference/model_router.py` — **Model Router** logic: try Primary (Qwen2-1.5B) → nếu fail → try Backup (Gemma 4 2B) → nếu fail → Fallback (template-based)
4: `{ai_services_generation}/src/inference/report_generator.py` — `NLPService.generate_report()` gọi pipeline: Aggregate → Enrich → Render Template → Model Router
5: Hỗ trợ `language`: `vi` (Việt) và `en` (English) — via prompt language instruction
6: Hỗ trợ `style`: `summary` (tóm tắt ≤500 từ, cluster-level) và `detailed` (per-anomaly explanation với semantic text)
7: Force Tone: professional audit tone (consulting-style recommendations: "Investigate missing transactions" thay vì "Bạn nên...")
8: Template-based fallback generate Markdown report từ Jinja2 template + Enriched data (không cần GPU)
9: Inference timeout ≤30s cho Qwen2-1.5B, ≤45s cho Gemma 4 2B
10: **After promotion** (Story 4.5): production API tại `{backend}/`:
    - `POST /api/v1/report/generate` body: `{analysis_id, language?, style?, model?}`
    - Report content lưu vào bảng `reports` (field `content` — Markdown, `model_used` — track model nào đã dùng)
11: Unit tests tại `{ai_services_generation}/tests/test_generation.py`

## Tasks / Subtasks

- [ ] Task 1: Create Jinja2 prompt template (AC: 1, 5, 6, 7)
  - [ ] Create `{ai_services_generation}/src/templates/` directory
  - [ ] Create `{ai_services_generation}/src/templates/report_prompt.j2` — LLM prompt template
  - [ ] Template structure: `### Instruction\n{instruction_text}\n### Input\n{enriched_json}\n### Output\n`
  - [ ] Vietnamese instruction variant: professional audit tone, Vietnamese section headers (Tóm tắt → Vấn đề chính → Phân tích chéo → Khuyến nghị)
  - [ ] English instruction variant: formal consulting tone (Executive Summary → Key Issues → Cross Analysis → Recommendations)
  - [ ] `summary` style: instruction limits output to ≤500 words, cluster-level only, no per-anomaly detail
  - [ ] `detailed` style: instruction requests per-anomaly explanation with semantic_text from enriched data
  - [ ] Tone enforcement in instruction: "Use professional audit language. Write recommendations as actionable directives (e.g., 'Investigate missing transactions') not suggestions."

- [ ] Task 2: Create fallback template (AC: 2, 8)
  - [ ] Create `{ai_services_generation}/src/templates/fallback_report.j2` — Jinja2 Markdown template
  - [ ] Template renders directly from EnrichmentResult data — no LLM required
  - [ ] Include sections: header (report title, date), Executive Summary (context summary), Key Issues (iterate clusters by priority), Cross Analysis (pre-computed equations), Recommendations (generic by issue_type)
  - [ ] Support `language` parameter: Vietnamese and English section headers
  - [ ] Support `style` parameter: `summary` (cluster-level) vs `detailed` (include anomaly details)
  - [ ] Output: well-formatted Markdown that matches LLM output structure

- [ ] Task 3: Implement Model Router (AC: 3, 9)
  - [ ] Create `{ai_services_generation}/src/inference/__init__.py`
  - [ ] Create `{ai_services_generation}/src/inference/model_router.py` with `ModelRouter` class
  - [ ] Implement `route(prompt, config) -> str` — try models in priority order:
    1. Primary: Qwen2-1.5B + LoRA adapter (timeout ≤30s)
    2. Backup: Gemma 4 2B + LoRA adapter (timeout ≤45s)
    3. Fallback: Template-based rendering (no GPU, always succeeds)
  - [ ] Implement `_try_model(model_name, prompt, timeout) -> str | None` — load model, generate, catch exceptions (OOM, timeout, service unavailable)
  - [ ] Implement `_fallback_generate(enriched_data, language, style) -> str` — render `fallback_report.j2` with enriched data
  - [ ] Load model configs (paths, timeouts, device) from `{ai_services_generation}/configs/generation_config.yaml`
  - [ ] Track which model was used: return `(report_content, model_used)` tuple
  - [ ] Support `model` parameter to force specific model (skip routing)

- [ ] Task 4: Implement Report Generator / NLPService (AC: 4)
  - [ ] Create `{ai_services_generation}/src/inference/report_generator.py` with `NLPService` class
  - [ ] Implement `generate_report(detection_result, language="vi", style="summary", model=None) -> ReportResult`
  - [ ] Pipeline: `AggregationService.aggregate()` → `EnrichmentService.enrich()` → render `report_prompt.j2` → `ModelRouter.route()`
  - [ ] Define `ReportResult` dataclass: `content: str` (Markdown), `model_used: str`, `language: str`, `style: str`, `generation_time_ms: int`
  - [ ] Import `AggregationService` from Story 4.1, `EnrichmentService` from Story 4.2
  - [ ] Measure and log generation time

- [ ] Task 5: Implement production API endpoint (AC: 10) — after promotion
  - [ ] Create `{backend}/app/api/endpoints/report.py`
  - [ ] Implement `POST /api/v1/report/generate` endpoint:
    - Request body: `{"analysis_id": int, "language": "vi"|"en", "style": "summary"|"detailed", "model": "qwen2"|"gemma4"|null}`
    - Load analysis result from database by `analysis_id`
    - Call `NLPService.generate_report()` with detection_result from analysis
    - Save report to `reports` table: `content` (Markdown), `model_used`, `analysis_id`, `language`, `style`
    - Return: `{"report_id": int, "content": str, "model_used": str}`
  - [ ] Register route in `{backend}/app/main.py` router
  - [ ] Use FastAPI `Depends()` for DB session injection
  - [ ] Add request validation with Pydantic schema

- [ ] Task 6: Write unit tests (AC: 11)
  - [ ] Create `{ai_services_generation}/tests/test_generation.py`
  - [ ] Test prompt template rendering with sample enriched data (both vi and en)
  - [ ] Test fallback template rendering — verify valid Markdown output
  - [ ] Test Model Router: mock primary model failure → verify backup called → mock backup failure → verify fallback called
  - [ ] Test NLPService full pipeline with mocked model (Aggregate → Enrich → Render → Route)
  - [ ] Test language parameter: verify Vietnamese vs English output headers
  - [ ] Test style parameter: verify summary (≤500 words) vs detailed (includes per-anomaly)
  - [ ] Test timeout enforcement: mock slow model → verify timeout triggers fallback

## Dev Notes

### Relevant Source Tree

```
csv_agent_platform/generation/
├── src/
│   ├── aggregation/
│   │   └── aggregation_service.py          ← Story 4.1: AggregationService (input)
│   ├── enrichment/
│   │   └── enrichment_service.py           ← Story 4.2: EnrichmentService (input)
│   ├── inference/
│   │   ├── model_router.py                 ← NEW: ModelRouter (Primary → Backup → Fallback)
│   │   └── report_generator.py             ← NEW: NLPService core
│   └── templates/
│       ├── report_prompt.j2                ← NEW: LLM prompt template
│       └── fallback_report.j2              ← NEW: Template-based fallback
├── configs/
│   └── generation_config.yaml              ← Model configs, timeouts, device settings
├── models/
│   ├── qwen2-1.5b-lora-adapter/           ← Story 4.3: Primary model
│   └── gemma4-2b-lora-adapter/            ← Story 4.4: Backup model
└── tests/
    └── test_generation.py                  ← NEW: Unit tests

csv_agent_services/backend/                 ← Production (after promotion)
├── app/
│   ├── api/
│   │   └── endpoints/
│   │       └── report.py                   ← NEW: Report generation endpoint
│   ├── services/
│   │   └── nlp_service.py                  ← NEW: Production NLPService (from Story 4.5 promotion)
│   ├── models/
│   │   └── report.py                       ← EXISTING: Report ORM model (report_url field)
│   └── ml/
│       └── generation/                     ← Story 4.5: Promoted inference code
│           ├── model_router.py
│           ├── report_generator.py
│           ├── adapter_weights/
│           └── templates/
```

### Patterns & Conventions

- **Backend architecture**: Layered — `endpoints/report.py` (controller) → `services/nlp_service.py` (service) → `ml/generation/` (ML layer)
- **FastAPI patterns**: `Depends()` for DB session, `HTTPException` for errors (400, 404, 500)
- **Error handling**: Structured logging via `get_logger()`, `HTTPException` raised after logging
- **API response format**: No standardized envelope — return raw JSON with relevant fields
- **Python naming**: `snake_case` for files, `PascalCase` for classes
- **Type hints**: `X | Y` union syntax (Python 3.10+)
- **Docstrings**: Google-style on all public methods
- **Config-driven**: Model paths, timeouts, device selection in `generation_config.yaml`
- **Jinja2 templates**: Standard Jinja2 syntax with `{{ variable }}` and `{% control %}`

### Tech Stack

- **Python** >=3.10
- **Jinja2** — prompt and fallback template rendering
- **PyTorch** >=2.0.0 — model inference
- **transformers** + **peft** — model loading with LoRA adapter
- **FastAPI** >=0.110.0 — production API endpoint (after promotion)
- **SQLAlchemy** >=2.0.29 — ORM for reports table
- **PyYAML** >=6.0 — config loading
- **pytest** >=7.4.0 — unit testing

### Notes from Previous Stories

- Story 4.1 `AggregationService` — first pipeline step, called by `NLPService.generate_report()`
- Story 4.2 `EnrichmentService` — second pipeline step, output used for both prompt rendering and fallback
- Story 4.3/4.4 adapter weights — loaded by `ModelRouter` from `models/` directory
- Story 4.5 promotion — `model_router.py` and `report_generator.py` are promoted to `{backend}/app/ml/generation/`
- Backend `report.py` model already exists with `report_url` field — may need migration to add `content`, `model_used`, `language`, `style` columns

### Testing

- **Test location**: `{ai_services_generation}/tests/test_generation.py`
- **Framework**: pytest >=7.4.0
- **Fixtures**: Mock model outputs, sample enriched data, expected Markdown reports
- **Test types**: Unit tests (template rendering, router logic), integration tests (full pipeline with mocked model)
- **Mocking**: Use `pytest-mock` to mock model inference — avoid requiring GPU in tests
- **Backend tests**: After promotion, add `{backend}/tests/test_report.py` for endpoint testing

## Change Log

| Date       | Version | Description   | Author        |
| ---------- | ------- | ------------- | ------------- |
| 2026-04-12 | 0.1     | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
