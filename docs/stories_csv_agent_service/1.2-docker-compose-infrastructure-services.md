# Story 1.2: Docker Compose & Infrastructure Services

## Status

Draft

## Story

**As a** developer,
**I want** all infrastructure services (MySQL, Redis, MinIO) running via Docker Compose,
**so that** the development environment is reproducible across all team members.

## Acceptance Criteria

1. `docker-compose.yml` định nghĩa 6 services: `backend`, `celery-worker`, `mysql`, `redis`, `minio`, `frontend`
2. MySQL 8.0 với volume persistent `mysqldata:/var/lib/mysql`
3. Redis 7 Alpine cho cả cache và Celery broker
4. MinIO với console truy cập tại port `9001`, data API tại port `9000`
5. Backend expose port `8000`, frontend expose port `3000`
6. `docker-compose up` khởi động thành công tất cả services không lỗi
7. Health check configured cho MySQL và Redis

## Tasks / Subtasks

- [ ] Task 1: Tạo `docker-compose.yml` với MySQL service (AC: 1, 2, 7)
  - [ ] Tạo `docker-compose.yml` tại project root (`giang_workspace/`)
  - [ ] MySQL service: `image: mysql:8.0`, `volumes: mysqldata:/var/lib/mysql`
  - [ ] Environment: `MYSQL_ROOT_PASSWORD`, `MYSQL_DATABASE=csvai`, `MYSQL_USER`, `MYSQL_PASSWORD`
  - [ ] Health check: `mysqladmin ping -h localhost`
  - [ ] Port: `3306:3306` (internal access only, nhưng expose cho dev debug)

- [ ] Task 2: Thêm Redis service (AC: 1, 3, 7)
  - [ ] Redis service: `image: redis:7-alpine`
  - [ ] Health check: `redis-cli ping`
  - [ ] Port: `6379` (internal only)

- [ ] Task 3: Thêm MinIO service (AC: 1, 4)
  - [ ] MinIO service: `image: minio/minio`
  - [ ] Command: `server /data --console-address ":9001"`
  - [ ] Ports: `9000:9000` (API), `9001:9001` (console)
  - [ ] Environment: `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`
  - [ ] Volume: `miniodata:/data`

- [ ] Task 4: Thêm Backend service (AC: 1, 5)
  - [ ] Backend service: `build: ./csv_agent_services/backend`
  - [ ] Port: `8000:8000`
  - [ ] `depends_on`: mysql (healthy), redis (healthy), minio
  - [ ] Environment: `DATABASE_URL=mysql+pymysql://user:pass@mysql:3306/csvai`, `REDIS_URL=redis://redis:6379/0`, `MINIO_ENDPOINT=minio:9000`
  - [ ] Command: `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`

- [ ] Task 5: Thêm Celery Worker service (AC: 1)
  - [ ] Celery worker service: `build: ./csv_agent_services/backend`
  - [ ] Command: `celery -A app.worker worker -l info`
  - [ ] `depends_on`: redis (healthy)
  - [ ] Chia sẻ cùng environment với backend

- [ ] Task 6: Thêm Frontend service (AC: 1, 5)
  - [ ] Frontend service: `build: ./csv_agent_services/fronted`
  - [ ] Port: `3000:3000`
  - [ ] Tạo minimal `{frontend}/Dockerfile`: `FROM node:18-alpine`, `WORKDIR /app`
  - [ ] Tạo minimal `{frontend}/package.json` placeholder

- [ ] Task 7: Thêm volumes declaration (AC: 2)
  - [ ] Declare named volumes: `mysqldata`, `miniodata`

- [ ] Task 8: Tạo `.env` file cho Docker Compose (AC: 1)
  - [ ] Tạo `.env` tại project root với default values cho dev
  - [ ] Tạo `.env.example` (without secrets) cho commit

- [ ] Task 9: Verify Docker Compose (AC: 6, 7)
  - [ ] `docker-compose up -d` — tất cả services start
  - [ ] `docker-compose ps` — tất cả services healthy
  - [ ] `curl http://localhost:8000/health` → 200
  - [ ] MinIO console: `http://localhost:9001` accessible
  - [ ] MySQL: `mysql -h 127.0.0.1 -u user -p csvai` connect thành công

## Dev Notes

### Relevant Source Tree

```
giang_workspace/                          # Project root
├── docker-compose.yml                    # NEW — Docker Compose file
├── .env                                  # NEW — Docker env vars (gitignored)
├── .env.example                          # NEW — Template env file
├── csv_agent_services/
│   ├── backend/                          # {backend}
│   │   ├── Dockerfile                    # From Story 1.1
│   │   └── app/                          # From Story 1.1
│   └── fronted/                          # {frontend}
│       ├── Dockerfile                    # NEW — minimal Next.js Dockerfile
│       └── package.json                  # NEW — placeholder
```

### Patterns & Conventions

- **Docker Compose version**: `3.8`
- **Service naming**: lowercase, single word (`backend`, `mysql`, `redis`, `minio`)
- **Health checks**: Required cho MySQL và Redis — backend `depends_on` with `condition: service_healthy`
- **Environment variables**: Injected via `.env` file hoặc `environment:` block
- **Volumes**: Named volumes cho persistence (not bind mounts)
- **Network**: Default bridge network — all services communicate by service name

### Tech Stack

- **Docker Compose**: 3.8
- **MySQL**: 8.0 — `mysql:8.0` official image
- **Redis**: 7 Alpine — `redis:7-alpine`
- **MinIO**: latest — `minio/minio`
- **Python**: 3.11-slim — backend base image
- **Node.js**: 18-alpine — frontend base image

### Notes from Previous Stories

- **Story 1.1**: Backend Dockerfile và app structure đã tạo. Docker Compose backend service build từ `./csv_agent_services/backend`.

### Testing

- **Test type**: Integration (Docker Compose up/down)
- **Manual verification**:
  - Tất cả containers running: `docker-compose ps`
  - MySQL connectable: `docker-compose exec mysql mysql -u root -p`
  - Redis pingable: `docker-compose exec redis redis-cli ping`
  - MinIO console accessible: browser `http://localhost:9001`
  - Backend health: `curl http://localhost:8000/health`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-18 | 0.1 | Initial draft | story-creator |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
