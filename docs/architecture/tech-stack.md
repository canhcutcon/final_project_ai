# Tech Stack

> Auto-generated by Shadow CLI — /scan skill.

## Project: `csv-agent-detection` (v0.1.0)

Source: `csv_agent_platform/detection/requirements.txt` + `setup.py`

## Technology Stack Table

| Category | Technology | Version | Purpose | Rationale |
|----------|-----------|---------|---------|-----------|
| **Runtime / Language** | Python | ≥ 3.10 | Primary language | Required for `X \| Y` union type syntax, `match` statements |
| **Data Processing** | pandas | ≥ 2.0.0 | Core DataFrame operations — load, clean, merge, feature engineering | Industry standard; v2.0 brings copy-on-write, pyarrow backend |
| **Numerical Computing** | NumPy | ≥ 1.24.0 | Array math, seed management, class weight computation | pandas dependency; used in `set_seed()` |
| **XLSX I/O** | openpyxl | ≥ 3.1.0 | Read/write `.xlsx` files | Pandas `read_excel(engine="openpyxl")` backend |
| **Machine Learning** | scikit-learn | ≥ 1.3.0 | Feature selection (MI), scalers (StandardScaler, MinMaxScaler), train/val/test split | Robust, well-tested classical ML toolkit |
| **Deep Learning** | PyTorch | ≥ 2.0.0 | Tensor export for anomaly detection models; device detection (CUDA/MPS/CPU) | Enables GPU training; `torch.save()` for `.pt` artifacts |
| **Columnar Storage** | PyArrow | ≥ 14.0.0 | Parquet read/write via `pandas.to_parquet()` | Efficient columnar format for large DataFrames |
| **Configuration** | PyYAML | ≥ 6.0 | Load `configs/default.yaml`, `column_mapping.yaml`, `business_rules.yaml` | Human-readable config; `yaml.safe_load()` only |
| **Time-Series Stats** | statsmodels | ≥ 0.14.0 | ADF stationarity test in `MetaFeatureEngineer` | Provides `adfuller()` for temporal feature extraction |
| **Parallelism** | joblib | ≥ 1.3.0 | Parallel preprocessing / model persistence (via sklearn) | sklearn dependency; used for scaler/selector `.pkl` saving |
| **Testing** | pytest | ≥ 7.4.0 | Unit tests for loader, schema, cleaning modules | Standard Python testing framework |
| **Containerization** | — | — | Not detected | No `Dockerfile`, `docker-compose.yml`, or `.devcontainer` found |
| **CI/CD** | — | — | Not detected | No GitHub Actions, GitLab CI, or Makefile found |
| **Monitoring / Logging** | Python `logging` | stdlib | Console stdout logging via `utils.helpers.get_logger()` | No external log aggregation (e.g., MLflow, W&B) detected |

## Device Acceleration

```python
# utils/helpers.py — get_device()
CUDA > MPS (Apple Silicon) > CPU
```

- Training targets GPU when available (CUDA/MPS auto-detected).
- PyTorch `>=2.0.0` required for MPS stability on macOS.

## Data Formats Used

| Format | Extension | Tool | Usage |
|--------|-----------|------|-------|
| CSV | `.csv` | pandas | Raw input (SNRE transaction reports) |
| Excel | `.xlsx` | openpyxl via pandas | Raw input (Prosage system exports) |
| Parquet | `.parquet` | PyArrow via pandas | Intermediate + final tabular outputs |
| PyTorch Tensor | `.pt` | `torch.save()` | Train/val/test tensor datasets for model training |
| YAML | `.yaml` | PyYAML | Config, column mapping, business rules |
| Pickle | `.pkl` | joblib | Saved scalers and feature selectors |

## Infrastructure Notes

- **Backend service** (`csv_agent_services/backend`): **empty** — not yet implemented.
- **Frontend** (`csv_agent_services/fronted`): **empty** — not yet implemented.
- **Generation service** (`csv_agent_platform/generation`): **empty** — not yet implemented.
- The entire active codebase lives in `csv_agent_platform/detection`.
