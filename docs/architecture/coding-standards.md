# Coding Standards

> Auto-generated by Shadow CLI — /scan skill.
> **Note:** Source code directories are currently empty (scaffolding phase). Standards below are derived from design documents and project configuration.

---

## General Rules

- **Language**: Python 3.11+ (backend, ML), TypeScript (frontend)
- **Formatting tools**: Ruff (Python linter/formatter — `.ruff_cache/` in `.gitignore`), ESLint/Prettier (frontend — inferred from Next.js stack)
- **Naming conventions**:
  - Python: `snake_case` for files, functions, variables; `PascalCase` for classes
  - TypeScript: `PascalCase` for components and classes; `camelCase` for functions and variables
- **File naming**: Lowercase with underscores for Python (`data_service.py`), PascalCase for React components (`CSVUploader.tsx`)
- **Import ordering**: Standard library → Third-party → Local (Python); framework → external → local (TypeScript)
- **Comment style**: Docstrings for Python classes/methods (triple-quote); JSDoc/inline for TypeScript
- **Git commit conventions**: Descriptive messages observed in history (`first commit`, `finish phase 1`, `update`)
- **Environment management**: `.env` files excluded via `.gitignore`; environment variables injected via Docker Compose

---

## Frontend Specific

### Component Structure
- **Organization**: Feature-based (pages map to features: `upload/`, `analysis/`, `report/`, `pipeline/`)
- **Component naming**: PascalCase, colocated with feature directories
- **Reusable UI**: Shared components in `{frontend}/src/components/ui/`
- **Feature components**: Grouped by domain — `csv/`, `analysis/`, `report/`, `pipeline/`
- **Pages**: Next.js App Router convention — `{frontend}/src/app/[feature]/page.tsx`

### Service Pattern
- **HTTP client**: Axios or Fetch wrapper in `{frontend}/src/lib/api.ts`
- **Custom hooks**: Feature-specific hooks in `{frontend}/src/hooks/` (`useUpload.ts`, `useAnalysis.ts`, `useWebSocket.ts`)
- **Business logic**: Lives in hooks and service layer, not in components
- **Pattern example** (from design docs):
  ```typescript
  // hooks/useUpload.ts — encapsulates upload logic
  // hooks/useAnalysis.ts — encapsulates anomaly detection state
  // hooks/useWebSocket.ts — real-time pipeline status
  ```

### State Management
- **Solution**: React hooks + custom hooks (no Redux/Zustand detected in design)
- **Local vs global**: Local state via `useState`/`useReducer`; shared state via custom hooks and context
- **Async state**: Handled through custom hooks wrapping API calls (loading/error/data pattern)
- **Real-time updates**: WebSocket hook for pipeline progress tracking

### Styling
- **Methodology**: CSS globals (`{frontend}/src/styles/globals.css`) — likely Tailwind CSS (standard Next.js 14 setup)
- **Theme usage**: Not explicitly detected; inferred to follow Next.js defaults

---

## Backend Specific

### Architecture Pattern
- **Pattern**: Layered architecture (Controller → Service → Repository/Model)
- **Structure**:
  - `api/v1/endpoints/` — Route handlers (controllers)
  - `services/` — Business logic (`DataService`, `AIService`, `NLPService`, `PipelineService`)
  - `models/` — SQLAlchemy ORM models
  - `schemas/` — Pydantic request/response DTOs
  - `ml/` — Machine learning models and inference
  - `core/` — Cross-cutting concerns (security, exceptions)
- **Dependency injection**: FastAPI's `Depends()` mechanism via `api/deps.py`
- **Async**: All service methods designed as `async def`

### Error Handling
- **Custom exceptions**: Defined in `{backend}/app/core/exceptions.py`
- **Propagation**: FastAPI exception handlers at the API layer; services raise domain-specific exceptions
- **Pipeline errors**: Tracked in `pipeline_runs` table with `error_message` field and `failed` status

### Validation
- **Request validation**: Pydantic schemas in `{backend}/app/schemas/` — enforced by FastAPI automatically
- **Database validation**: SQLAlchemy model constraints (NOT NULL, foreign keys, data types)
- **Pipeline validation**: Config validation via `PipelineConfig` Pydantic model before task submission

---

## API Response Format

### Standard Endpoints
- **Success**: Direct JSON response with data payload
- **Error**: HTTP status codes with error message body
- **Pagination**: Not explicitly documented; likely offset-based given PostgreSQL backend

### Async Pipeline Endpoints
- **Submit**: Returns `pipeline_id` (UUID) for tracking
- **Status polling**: `GET /api/v1/pipeline/{id}/status` returns current step and status (`pending`/`running`/`completed`/`failed`)

### HTTP Status Conventions
| Status | Usage |
|--------|-------|
| `200` | Successful GET/query |
| `201` | Successful resource creation (upload, analysis run) |
| `202` | Accepted — pipeline submitted for async processing |
| `404` | Resource not found |
| `422` | Validation error (Pydantic schema mismatch) |
| `500` | Internal server error |

### API Versioning
- Prefix: `/api/v1/` — versioned from the start
