# Source Tree

> Auto-generated by Shadow CLI — /scan skill. Max depth: 4 levels.
> Aliases: `{frontend}` = `csv_agent_services/fronted`, `{backend}` = `csv_agent_services/backend`,
> `{ai_services_detection}` = `csv_agent_platform/detection`, `{ai_services_generation}` = `csv_agent_platform/generation`.
> Skipped: node_modules, .git, .next, .swc, __pycache__, .pytest_cache, venv, dist/build, *.lock, *.zip, .DS_Store.

## `csv_agent_services` — the product (API + UI)

```
csv_agent_services/
├── docker-compose.yml                # MySQL · Redis · MinIO · backend · celery_worker · frontend
├── readme.md                         # run instructions, ports, API table, model table
├── docs/                             # product PRDs / notes
│   ├── click-into-generate-report-abstract-flute.md
│   └── detection-data-processing-pipeline-prd.md
│
├── backend/                          # {backend} — FastAPI + SQLAlchemy + Celery
│   ├── Dockerfile                    # backend container image
│   ├── requirements.txt              # Python deps (FastAPI, SQLAlchemy, Celery, xgboost, torch…)
│   ├── pytest.ini                    # pytest config
│   ├── alembic.ini                   # Alembic migration config
│   ├── .env / .env.example           # runtime config (DB/Redis/MinIO/JWT/Gemini)
│   ├── alembic/
│   │   ├── env.py                    # migration environment
│   │   └── versions/                 # 11 revisions existed (init → templates/config) but only
│   │                                 #   compiled __pycache__ remains — .py sources are gone from
│   │                                 #   the working tree; `alembic upgrade head` will not work as-is
│   ├── app/
│   │   ├── main.py                   # FastAPI app + CORS + router registration + /health
│   │   ├── api/
│   │   │   └── endpoints/            # upload, analysis, datasets, pipeline, report,
│   │   │                             #   dashboard, agent, review, cases, validate, rules, templates
│   │   ├── core/                     # config.py (settings), celery_app.py, logger.py
│   │   ├── db/
│   │   │   └── session.py            # async engine + get_db dependency
│   │   ├── models/                   # SQLAlchemy ORM: dataset, analysis_result, report,
│   │   │                             #   pipeline_run, anomaly_case, suspicion_rule,
│   │   │                             #   data_fix_log, review_queue, template, base
│   │   ├── schemas/                  # Pydantic DTOs: analysis, case, pipeline, rule,
│   │   │                             #   validation, evidence_packet
│   │   ├── services/                 # business logic
│   │   │   ├── ai_service.py         # AIService.detect — V11/V10 inference + explainability
│   │   │   ├── data_service.py       # CSV preprocess + data-type detection
│   │   │   ├── nlp_service.py        # report generation + PDF export
│   │   │   ├── gemini_fix_service.py # Gemini fix suggestions
│   │   │   ├── validation_service.py # rule-based validation
│   │   │   └── storage.py            # MinIO client wrapper (minio_service)
│   │   ├── tasks/
│   │   │   ├── pipeline_tasks.py     # 5-task Celery chain + launch_pipeline
│   │   │   └── agent_fix_task.py     # async Gemini fix task
│   │   └── ml/                       # serving-side ML (mirrors platform)
│   │       ├── models/               # model_selector, xgboost_detector, bilstm_classifier,
│   │       │   ├── ...               #   dae_encoder, universal_features, model_metadata, base_model
│   │       │   └── v10/ , v11/       # served model weights (.pt / .json)
│   │       ├── generation/           # model_router, report_generator, pdf_engine + templates/*.j2
│   │       └── utils/                # io.py, helpers.py
│   ├── scripts/
│   │   └── benchmark_latency.py      # latency benchmark
│   └── tests/                        # pytest: upload, database, data_services, main,
│                                     #   pdf_export, report_fidelity, router_accuracy
│
└── fronted/                          # {frontend} — Next.js 14 App Router (note: spelled "fronted")
    ├── package.json                  # next 14.2 · react 18 · tailwind · jest
    ├── next.config.mjs / tsconfig.json / tailwind.config.ts / postcss.config.mjs
    ├── jest.config.ts / jest.setup.ts / .eslintrc.json
    ├── .env.local                    # NEXT_PUBLIC_API_BASE_URL / WS_URL
    ├── app/
    │   ├── layout.tsx                # root layout: SideNavBar + TopAppBar
    │   ├── page.tsx                  # landing page
    │   ├── globals.css / fonts/
    │   └── (pages)/                  # route group
    │       ├── dashboard/page.tsx
    │       ├── upload/page.tsx       # 4-step upload wizard
    │       ├── analyses/page.tsx + [id]/page.tsx
    │       ├── pipeline/[id]/page.tsx
    │       ├── reports/page.tsx + [id]/page.tsx
    │       └── templates/page.tsx
    ├── components/
    │   ├── anomaly/                  # drill-down, case action bar, status chip, feature bars,
    │   │                             #   expected-vs-actual, model metadata, assign/trigger modals
    │   ├── upload/                   # wizard stepper, schema mapping, column config,
    │   │                             #   validation summary, threshold slider, rule editor, templates
    │   ├── layout/                   # SideNavBar, TopAppBar
    │   ├── templates/               # EditTemplateModal
    │   ├── GeminiFixModal.tsx · PipelineProgress.tsx · StartAnalysisModal.tsx
    ├── lib/api.ts                    # all backend calls (apiFetch wrapper + mappers)
    ├── hooks/useWebSocket.ts         # live pipeline updates
    ├── types/upload.ts               # shared TS types
    ├── __tests__/                    # api.test.ts, SideNavBar/TopAppBar tests
    └── UI/                           # design mockups (HTML + DESIGN.md + screenshots)
```

## `csv_agent_platform` — the ML factory (offline)

```
csv_agent_platform/
├── detection/                        # {ai_services_detection} — anomaly-detection ML
│   ├── setup.py                      # packaging (csv-agent-detection)
│   ├── requirements.txt              # pandas, torch, xgboost, scikit-learn, statsmodels, tabpfn
│   ├── TASKS_PROCESSDATA.md          # pipeline task notes
│   ├── configs/
│   │   ├── default.yaml              # 8-phase pipeline + labels + domain rules + splitting
│   │   ├── business_rules.yaml       # business-rule validation config
│   │   ├── column_mapping.yaml       # schema standardization map
│   │   └── v10_retrain.yaml
│   ├── src/
│   │   ├── data/                     # 27 modules: loader, schema, cleaning, merger, profiler,
│   │   │                             #   features_tabular/temporal/meta, label_engineering,
│   │   │                             #   synthetic, timeseries, windowing, splitter, transforms,
│   │   │                             #   feature_selection, pseudo_labeling, validation, dataset…
│   │   ├── models/                   # xgboost_detector, bilstm_classifier, dae_encoder,
│   │   │                             #   tranad, anogan, learned_hash, base_model
│   │   ├── agent/                    # auto_fix_engine, gemini_agent, fix_cache, revalidation
│   │   ├── rules/                    # rule_scorer
│   │   ├── evaluation/ · training/ · utils/ (io, helpers)
│   ├── scripts/                      # run_pipeline.py (8-phase CLI) + evaluate/postprocess/dump
│   ├── notebooks/                    # train_v11_universal, train_v10_retrain, ablations,
│   │                                 #   benchmarks, cross-schema transfer, result notebooks
│   ├── models/                       # trained artifacts: v10/, v11/, *_retrain/, scalers (.pkl)
│   ├── data/                         # raw/, raw_anonymized/, data_anonymized/, processed/,
│   │                                 #   quarantine/, benchmark_high_dim/
│   ├── outputs/                      # ablation/benchmark JSON + plots + logs
│   └── tests/
│
├── generation/                       # {ai_services_generation} — report-generation ML
│   ├── configs/generation_config.yaml# aggregation/enrichment/inference/training/eval + prompts
│   ├── src/
│   │   ├── aggregation/              # aggregation_service.py (cluster representative samples)
│   │   ├── enrichment/               # enrichment_service.py (impact/risk scoring)
│   │   ├── inference/                # model_router.py, report_generator.py
│   │   ├── templates/               # report_prompt.j2, fallback_report.j2
│   │   └── training/                 # train_qwen_lora.py, train_gemma_lora.py,
│   │                                 #   data_loader.py, evaluate.py
│   ├── models/                       # qwen2-1.5b-lora-adapter/, gemma4-2b-lora-adapter/
│   ├── scripts/promote_model.py      # model promotion gate
│   ├── utils/helpers.py
│   ├── notebooks/                    # train + zero-shot evaluation notebooks
│   ├── data/                         # report-sample/, training/
│   ├── outputs/                      # training metrics/curves + evaluation decisions
│   └── tests/                        # test_aggregation/enrichment/generation/evaluate/data_loader
│
└── outputs/                          # cross-repo eval notebooks (qwen/gemma) + v7
```
